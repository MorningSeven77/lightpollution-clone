@AGENTS.md

# 项目说明

练手项目：复刻 [lightpollutionmap.app](https://lightpollutionmap.app) 的核心功能（光污染地图 + Bortle/SQM 点位查询），目标是学习全栈开发流程和 Claude Code 建站工作流。用户是全栈新手，边做边学，讲解时倾向解释概念而不是只给结论。

## 技术栈

- Next.js (App Router，webpack 而非 Turbopack，见下方"已知坑点") + TypeScript + Tailwind CSS v4
- 地图：MapLibre GL JS v6（免费开源，无需 API key）
- 光污染数据：Google Earth Engine（NOAA VIIRS 卫星年度合成数据 `NOAA/VIIRS/DNB/ANNUAL_V22`），服务端通过服务账号认证调用
- 地点搜索：Nominatim (OpenStreetMap) 地理编码，经后端 API 路由代理
- 部署：Vercel，代码仓库 [MorningSeven77/lightpollution-clone](https://github.com/MorningSeven77/lightpollution-clone)，线上地址 https://lightpollution-clone.vercel.app

## 目录约定

- `src/app/api/*/route.ts` — 后端接口（geocode 代理、Earth Engine 瓦片/点位查询）
- `src/components/` — React 组件（`Map.tsx` 是核心，受控组件，见下方）
- `src/lib/` — 工具函数与第三方服务封装（Earth Engine 认证、Bortle/SQM 换算、URL 状态同步、配色/底图配置）
- `scripts/copy-maplibre-worker.mjs` — postinstall 时把 maplibre worker 文件复制到 `public/maplibre/`，不要删

## 当前进度

- **MVP 闭环**：地图 + 真实 VIIRS 光污染叠加层 + 点击查询 Bortle/SQM + 地点搜索 + URL 分享，全部完成并部署上线
- **批次二·视觉改版**：4 套配色风格（经典/柔光/科幻/群光）+ 底图切换（暗色/浅色/彩色）+ 透明度滑块 + 右上角设置面板，全部完成并部署上线
- **批次三·第一项·位置详情面板**：点击地图弹出左侧常驻面板（坐标、反查地名、大字 Bortle、SQM、粗略可见恒星数），替换了原来的小气泡，全部完成并部署上线
- **批次三·第二项·历史趋势图**：位置详情面板里 Bortle/SQM 下方加了柱状趋势图（`src/components/TrendChart.tsx` + `src/app/api/trend/route.ts`），合并 VIIRS V21（2013-2021）+ V22（2022+）两个年度合成数据集，一次 `getRegion()` 拿到某个点全部年份的数据。已完成。
- **街道文字太粗**：底图 CJK 字体回退到较粗的字重，用 `text-halo-width` 调细（`Map.tsx` 的 `thinOutLabels`）。已完成。
- **世界视图下颜色图层太稀疏**：`tile-layer/route.ts` 生成瓦片的图像加了 `reduceResolution({reducer: ee.Reducer.max(), maxPixels: 1024, bestEffort: true})`，缩到最小时也能看到连成片的亮色区域，不再是稀疏光点。已完成，细节见下方已知坑点。
- **底图/配色风格对齐竞品**：用真实 Chrome 打开 lightpollutionmap.app 逐项核实过实际效果（不能只信文字转述，见下方坑点）——底图选项改成"街道/暗色/卫星图"（`src/lib/basemapStyles.ts`，卫星图接的是免费无需 key 的 Esri World Imagery 栅格瓦片），配色风格改名对齐"经典/高亮/辉光/科研"并配上标语和对应调色板（`src/lib/colorStyles.ts`），图例区分 Bortle 离散色块（经典专用）和连续 SQM 数值刻度（其余三个共用，`src/components/Legend.tsx`），顺带修了设置面板"光污染图层"开关按钮圆点没居中的样式 bug（`src/components/MapControls.tsx`）。已完成。
- **"经典"改成离散分区渲染，不再是平滑渐变，调色板也换成从竞品瓦片里实测的真实值**：用户发了竞品截图，"经典"风格实际是像等高线一样一圈圈边界清晰的色带，不是平滑模糊过渡——这其实是"经典＝清晰分区"这个标语本来就该有的效果，只是最初实现时一直是平滑渐变，跟标语对不上。修法是在 `tile-layer/route.ts` 里 `visualize()` 之前，先 `image.focal_mean({radius: 4, units: "pixels"})` 抹掉像素级噪声（不这么做的话，离散化会把每个带噪声的像素各自量化到不同色阶，看起来像雪花噪点而不是干净的等高线——必须先平滑连续信号再离散化，反过来"先离散化再平滑"只会把已经量化好的色块边界重新糊在一起），再把连续辐射值离散化成分区，最后喂给调色板——`visualize()` 本身没有"色阶化"选项，所以离散化必须发生在输入值上，不是调色板或 visParams 层面能做到的。只对 `legendType === "bortle"` 的风格（目前只有经典）生效，其余几个风格标语本来就是"平滑强度"/"天光扩散"这种明确要连续渐变的，不受影响。

  **分区边界不能用均匀步长，得用真实 Bortle 等级的辐射值断点，不然缩小地图后大部分区域会变成纯黑**：最初拿 `(max-min)/9` 均匀切 9 段（`image.subtract(min).divide(step).floor()...`），结果用户反馈"缩放后就变成点了"——均匀切分在 `min=0,max=150` 下，第一段的门槛是 16.7，而 `bortle.ts` 自己的辐射→SQM 换算是对数关系（这个关系本来就是为了压缩 VIIRS 那种跨几个数量级的辐射范围），意味着大部分有人居住但不算特别亮的地区（辐射值往往在个位数到十几）全部落进"纯黑"这一档，只有真正的城市核心才亮得起来，看起来就是黑底上撒了几个点。修法是把 `bortle.ts` 里已有的 `BORTLE_SQM_THRESHOLDS`（Bortle 1-9 对应的真实 SQM 门槛）反过来通过 `sqmToRadiance()` 换算成辐射值断点（新增导出 `BORTLE_CLASS_RADIANCE_BREAKPOINTS`），越靠近暗端断点挤得越密（1-4 级之间辐射差距只有几个单位），越亮断点分得越开——用 `image.gte(breakpoint)` 累加算出每个像素落在哪一档（这是 EE 里常见的"数值分箱"手法：8 个 `gte` 布尔图层加起来正好就是 0-8 的分类索引），比均匀切分更贴近数据实际分布，微弱的光也能显出颜色而不是直接归零。**教训**：给连续物理量分档，不能想当然用均匀步长，得看清楚底层数值分布是不是线性的——这已经是这个项目里第二次犯类似的错（上一次是"高亮/辉光/科研"三个风格的固定 min/max 范围）。

  这次顺带走了个弯路值得记录：一开始以为问题出在 `reduceResolution` 的低 `maxPixels`（64）导致低缩放级别"漏采样"，试过用 `focal_max` 按固定真实距离（几公里）膨胀亮点、甚至把 `focal_max` 和 `reduceResolution`叠在一起用，结果叠加后耗时不降反升还很不稳定（同一个 z=6 瓦片有次量到 27 秒），而且 `focal_max` 的膨胀半径调小到不影响世界视图效果时，对城市级别的细节还是有明显的"糊成一坨"副作用。最后用不膨胀、只有 `reduceResolution(64)` 的版本重新测了一遍世界视图，效果跟膨胀版本一模一样——才发现真正救场的是上面说的断点分类，`focal_max` 全程都是无效功、白费的复杂度。**教训**：怀疑某个改动是问题根源之前，先控制变量单独测一下"去掉这个改动，问题是不是还在"，不要在多个改动叠加的情况下直接调参数猜——不然很容易在错误的地方越调越复杂。

  调色板本身也按上面"科研"用过的抓瓦片手法重新测了一遍（这次是 `image-tiles/2025/{z}/{x}/{y}.png`，不带 style 前缀——**竞品默认风格 Classic 走的是老的无 style 前缀路径，其他非默认风格才走 `style-tiles/{styleId}/...`，两条路径别搞混，用错了会直接 404 或者神秘的 fetch 失败**）。同时发现他们瓦片里最大 zoom 只到 6，再往上都是客户端拉伸同一张 z6 图（不是真的更高分辨率瓦片）。抓回来的真实调色板只有约 9-11 种纯色（`000000/222222/142f72/2154d8/0f5714/1fa12a/6e641e/b8a625/bf641e/fb5a49/f2f2f2`），另外亮区里散布着一些跟渐变顺序完全对不上的纯灰色小色块，判断是"已知聚居点"之类的独立标记图层，不是配色本身的一部分，没有照搬。**顺带踩的坑**：新调色板顶端换成了真正的白色（`f2f2f2`）之后，如果 `max` 还沿用旧值 60，实测开罗中心真实辐射值有 95+，会导致一大片区域全部顶到最亮色阶变成一片死白——把 `max` 提到 150 才让白色只出现在真正最亮的核心，跟竞品截图观感一致。**教训**：换调色板（尤其换了顶端颜色更极端/更刺眼的颜色，比如从粉色换成纯白）之后，必须重新核对 `min`/`max` 会不会导致大片真实数据被裁到顶——不是配色本身的问题，是配色和取值范围要配套调。
- **讨论过、明确不做的事**：离线预渲染瓦片金字塔（像竞品那样把每个缩放级别提前算好存成静态图，能同时解决"实时算太慢"和"低缩放级别不够精细"两个问题，但要多一套 Earth Engine 批量导出 + 切瓦片 + 静态托管的流程，跟现在"实时调 API"的架构完全不同，粗估工作量大半天到一天、中间还有几个小时级别的导出等待）。跟用户讨论过大致工作量和收益后，用户选择跳过，继续用现在的实时计算方案。以后如果又嫌"精细度"或"速度"不够，不用重新分析问题——根因已经很清楚了（架构限制，不是参数能调出来的），直接翻这条记录就行，除非用户明确想重新考虑这个方向。
- **暗空地点图层**：接入 DarkSky International 官方认证的暗空地点（Park/Community/Sanctuary/Reserve/Urban Night Sky Place 五类），设置面板加"认证暗空地点"开关（`MapControls.tsx`）+ 分类颜色图例，`Map.tsx` 加一个 GeoJSON circle 图层（`ensureDarkSkyPlacesLayer`），点击某个点位会弹出 `DarkSkyPlacePanel.tsx`（名称/分类/认证年份/保护面积/官网链接），跟点击查询 Bortle/SQM 的 `LocationDetailPanel` 是互斥的两个面板（点其中一个会关掉另一个，位置显示`page.tsx`里`selectedDarkSkyPlace`/`selectedLocation`两个 state 互相清空）。数据是**离线一次性抓取**的静态 JSON（`src/data/darkSkyPlaces.json`，267 条），不是运行时请求 darksky.org——原因和抓取方法见下方坑点。已完成，颜色图层的渲染效果没能在本机浏览到（见下方坑点），部署后请用户实测确认一遍。
- **极光图层**：**竞品的极光功能其实不在主地图里**——点开是跳到一个独立姊妹站 auroramap.app，用的是南北极点的极射投影视图，跟咱们的世界地图完全是两套 UI，跟用户确认过之后决定不照搬这个架构，改成在现有世界地图上加一层可开关的极光概率叠加。数据源是 NOAA 太空天气预报中心的 OVATION 极光模型（`https://services.swpc.noaa.gov/json/ovation_aurora_latest.json`，公开无需鉴权，5-15 分钟更新一次），新增 `src/app/api/aurora/route.ts` 服务端代理并转成 GeoJSON（**NOAA 经度是 0-359，必须转成 -180..180 不然东半球会整体偏移一整圈**；同时过滤掉概率为 0 的点，能把 65160 个点砍到约 18000 个）。`Map.tsx` 用 MapLibre 的 `heatmap` 图层类型渲染（`ensureAuroraLayer`），配色绿→黄→红对齐竞品自己极光页图例的 10%/50%/90% 三档。数据只在开关打开时按需拉取一次并缓存（不是每次开关切换都重新请求，也不是页面一加载就拉），跟 VIIRS 瓦片同一个"按需 + 缓存"模式，只是极光只有一份数据不需要按 key 分桶缓存。已完成，`tsc`/`lint`/`build` 全过，`/api/aurora` 直接 curl 测过返回正常（18033 个点，经度已正确归一化），但图层本身的可视化效果同样没能在本机肉眼确认（见下方坑点），请实测。
- **夜间天气预报面板**：又是一个"实测发现跟想象不一样"的例子——竞品的"天气"根本不是地图图层，而是点击地图后已有的 Location Details 侧边栏里多出的一个"Nighttime Weather"区块（展示未来几天该点的夜间观星天气），所以这次没碰 `page.tsx`/`MapControls.tsx`/`Map.tsx`，纯粹是给 `LocationDetailPanel.tsx` 加一块。数据源是 Open-Meteo（免费不需要 key），新增 `src/lib/weather.ts`（WMO 天气码→emoji/中文、月相→照亮度的余弦公式、`hourly.is_day` 分桶算"某天晚上"的夜间云量）+ `src/lib/stargazing.ts`（云量/月光/Bortle 三项扣分的观星评分公式，跟 `bortle.ts` 一样明确写清楚是简化近似）+ `src/app/api/weather/route.ts`（代理 Open-Meteo）+ `src/components/WeatherSection.tsx`（展示组件）。**评分特意放在前端 `useMemo` 算，不放进 `/api/weather` 接口**——`LocationDetailPanel.tsx` 现有的 `useEffect` 是四个 `fetch*` 并行发起互不等待，如果天气接口需要先知道 Bortle 等级才能算分，就得等 `point-value` 先返回，破坏现有的并行结构；改成接口只返回原始天气数据，两边数据各自到位后用 `useMemo` 合并算分，两个请求继续保持独立并行。已完成，而且这次**是三个新功能里唯一一个真正做到端到端可视化验证**的——因为这个面板是纯 React DOM，不依赖 MapLibre 的 WebGL 渲染循环，不受下面坑点里那个"预览面板不合成帧"问题影响，直接点开纽约的位置详情，天气区块的 7 张卡片、最佳观测夜徽章、SQM/Bortle 数字全部对得上。
- **昼夜分界线**：用真实 Chrome 核实过，这个在竞品是 Map Controls 面板里一个正经的图层开关（"Day/Night Terminator" / "Show day/night areas"），不像极光/天气那样另有隐情。跟前几个功能不一样的是**完全不需要接外部数据源**——晨昏线是纯天文计算，新增 `src/lib/terminator.ts`：太阳赤纬用"day of year"代入余弦公式（准确度约 ±0.2°），太阳直射点经度加了时差方程（equation of time）修正，晨昏线上每经度对应纬度用标准公式 `tan(lat) = -cos(hourAngle)/tan(declination)` 算出，首尾用南/北极点闭合成一个覆盖"夜半球"的多边形（`declination>=0` 时闭合南极，反之闭合北极——北半球夏天南极点在极夜，是这么判断的）。`Map.tsx` 用 `fill` 图层渲染这个多边形（`ensureTerminatorLayer`），开关打开时每 60 秒 `setData()` 重新算一次多边形保持跟着太阳走（晨昏线每小时移动约 15° 经度，不刷新的话放久了会肉眼可见地"过期"）。**这次的验证比前几个都更扎实**：写了一版独立测试脚本，用夏至/冬至/春分三个已知日期代入验证太阳赤纬输出（分别应该是 +23.44°/-23.44°/≈0°，实测完全对上），又验证了太阳直射点纬线上晨昏线纬度应该精确等于 `-(90°-赤纬)`（实测精确对上，误差为0）——数值上的正确性比截图肉眼核对更可靠，因为这次尝试放大竞品世界地图核实视觉效果时缩放操作在真实 Chrome 里失败了（鼠标滚轮对那张地图完全没反应，原因不明，放弃继续排查）。已完成，`tsc`/`lint`/`build` 全过，开关切换在浏览器里也确认没有报错。
- **天气叠加图层（云层/降水）**：原定四项之外顺手发现的——竞品 Map Controls 面板里有个标"NEW"的"Weather Overlay"下拉框（None/Clouds/Rain），跟已经做的"夜间天气预报面板"是完全不同的两个东西（一个是点位天气预报，这个是叠在世界地图上的云图/雨图）。抓包确认是 OpenWeatherMap 的瓦片图层（`tile.openweathermap.org/map/clouds_new/{z}/{x}/{y}.png?appid=...`）——**这是目前唯一一个需要 API key 的第三方数据源**（前面几个：Nominatim/NOAA/Open-Meteo/DarkSky REST 全部免费免 key）。新增 `src/lib/weatherOverlay.ts`（none/clouds/rain 三态配置）+ `src/app/api/weather-tiles/route.ts`（服务端代理，key 从 `OPENWEATHERMAP_API_KEY` 环境变量读，**故意不学竞品直接把 key 暴露在客户端瓦片 URL 里**——那样任何人翻 Network 面板都能拿走别人的 key，代理到自己后端才是对的做法，参考 Earth Engine 服务账号密钥已经是这么处理的）+ `Map.tsx` 加一个三态 raster 图层（`ensureWeatherTilesLayer`，none 时隐藏图层而不是整个移除，切回 clouds/rain 不用重新 addLayer）+ `MapControls.tsx` 加下拉选择器（不是开关，跟竞品一样是三选一）。`tsc`/`lint`/`build` 全过，用真实浏览器点了下拉框验证过：瓦片请求确实带上了正确的 z/x/y/layer 参数，图层管线本身是通的。**用户已经把 key 配进 `.env.local` 了，但 OpenWeatherMap 那边直接返回 401 "Invalid API key"**——不是咱们代码的问题，直接拿这个 key 单独 curl OpenWeatherMap 官方接口复现了一模一样的 401，判断是新注册的 key 还没激活（OpenWeatherMap 官方文档写的是新 key 生效有几分钟到最多 2 小时的延迟），用户决定先跳过等会儿再看。**下次回来处理时**：先直接 `curl "https://tile.openweathermap.org/map/clouds_new/2/2/1.png?appid=<key>"` 测一下 key 本身激活了没有，如果还是 401 就是还没等够，激活了的话应该会直接返回一张 PNG 图片，到时候 `.env.local` 里的 key 不用改，重启一下 dev server（`OPENWEATHERMAP_API_KEY` 是进程启动时读的，改完 .env.local 光靠 Next.js 热更新不会生效）直接点地图控制面板里的"天气叠加"下拉框选云层/降水验证。见下方"注意事项"里 key 怎么配。
- **月相面板**：又是工具栏顺手发现的功能（点了竞品工具栏上一个月牙图标，弹出"Lunar Phase"面板）——相位名称（如"Last Quarter"）、月龄、照亮度%、月出月落时间+倒计时、下四个关键相位日期，还有距离/地平高度两个数字（这两个需要真正的月球星历表才能精确算，比太阳位置复杂不少，这次**明确跳过没做**，跟月出月落时间一起——月出月落需要绑定一个具体地点，工具栏这个面板不像点位详情面板那样有"当前选中点"，加对应复杂度不值得，第一版只做不依赖地点的部分：相位名称/月龄/照亮度/下次关键相位日期）。新增 `src/lib/moonPhase.ts`：月相是标准朔望月周期计算（`SYNODIC_MONTH_DAYS≈29.53` 天 + 一个已知新月参考时刻，逐月周期取模），复用 `weather.ts` 里已经写好的相位分数→照亮度余弦公式（不用重复造轮子）。**验证比预期精确**：拿真实 Chrome 上竞品当时显示的真实数据核对——照亮度/月龄数值级别对齐（差距在合理近似误差范围内，跟太阳赤纬近似公式一个道理），"下次新月"和"下次上弦月"两个日期**算出来的跟竞品显示的日期完全一样**（8/13 和 8/20 左右，仅一天误差在容忍范围），说明简化的朔望月周期公式在日期精度上已经够用。组件 `MoonPhasePanel.tsx` 是独立悬浮面板（不需要 props，自己管理开关状态），**踩了个布局坑**：一开始随手放在 `fixed right-4 top-4`，用 `getBoundingClientRect()` 实测发现直接和 MapLibre 自带的缩放控件（`.maplibregl-ctrl-top-right`）重叠，而且 `right-4` 这一列在 `MapControls` 展开时整条竖着都被占满了——最后挪到 `fixed left-1/2 top-4`（顶部居中），这一片是唯一没被 SearchBar/MapControls/缩放控件占用的地方。**教训**：加新的悬浮 UI 元素之前，最好先用 `getBoundingClientRect()` 或 `read_page` 实际量一下已有元素的位置，不要凭截图或直觉估坐标——这次两次估计（先猜 right-4 top-4，改猜 left-1/3）都算错了，第三次才用实测数据选对位置。已完成，`tsc`/`lint`/`build` 全过，浏览器里点开验证过面板内容和控制台无报错。
- **曝光计算器（简化版）**："EXP" 图标点开是"Sub-exposure Planner"——输入天空亮度（自动带入当前点位 SQM）、滤镜、相机类型、焦比，算出建议的单帧曝光时长（让读出噪声不超过总噪声预算的某个百分比，这是天文摄影里真实的"sub-exposure calculator"概念，竞品还有一大堆进阶参数：像素尺寸/读出噪声/传感器量子效率/暗电流/镜头透过率/单帧上限/可用时长）。这个比前面几个天文位置计算复杂得多——不是成熟的标准公式（太阳/月球位置这种），核心的"SQM→每像素每秒电子数"转换涉及具体的光度学零点常数，容易算错还显得煞有介事，所以**明确跟用户确认过做简化版，不追求和竞品数值一致**。噪声预算部分（读出噪声² = 目标占比² ×（天光噪声+暗电流噪声）×曝光时间，解出时间）是真实成熟的 CCD/CMOS 噪声理论，这部分没打折扣；唯一简化/校准的是"天光信号强度"怎么算——直接复用项目里已有的 `sqmToRadiance()`（`bortle.ts`，本来就用来算光污染配色的）当天光亮度的代理指标，再乘以像素尺寸²、量子效率、镜头透过率、除以焦比²（这个 1/焦比² 关系是真实光学原理——对天光这种展源，每像素收光量只看焦比不看口径，所以竞品界面也只让填焦比不问口径/焦距），最后有一个校准常数（`CALIBRATION_CONSTANT`）是拿竞品当时实测的一个真实数据点（SQM 17.23, f/5, 制冷 CMOS → 18秒）反推校准的，不是凭空拍的，代码注释里写清楚了这是经验校准不是物理推导。新增 `src/lib/exposureCalculator.ts`（三档相机预设 + 计算函数）+ `src/components/ExposureCalculator.tsx`（纯前端实时计算，不需要接口，`useMemo` 随输入变化即时刷新），接进 `LocationDetailPanel.tsx`（`TrendChart`/`WeatherSection` 下面），用当前点位的 SQM 自动带入。已完成，`tsc`/`lint`/`build` 全过，浏览器里验证过点击位置后曝光时长正确显示、切换相机类型/焦比后数字实时联动更新（f/5→f/2.8 时从 20s 降到 6.2s，方向正确），控制台无报错。
- **位置历史 + 位置对比**：竞品 Location Details 面板里的"History"标签点开是"Location History"弹窗（最近10条查询记录，每条带"Compare"按钮），点 Compare 打开"Location Comparison"弹窗（最多5个地点，按SQM从暗到亮排名，显示"Sky background"倍数）。这两个功能天然配套，用户确认一起做。**纯前端 localStorage，不需要新接口**：新增 `src/lib/locationHistory.ts`（`getLocationHistory`/`addLocationHistoryEntry`/`clearLocationHistory`，按坐标去重、最多存10条；`computeSkyBackgroundRatio(darkestSqm, sqm)` 用星等制标准公式 `10^((darkestSqm-sqm)/2.5)` 算亮度倍数——5等星差=100倍亮度，这是星等制的数学定义本身，不是近似值），`LocationDetailPanel.tsx` 里查询成功（Bortle/SQM + 地名都到位）后自动写一条历史。**没有做成独立的"History"+"Compare"两个入口**，而是合并成一个 `LocationHistoryPanel.tsx`：历史列表里每条自带一个"加入对比"复选框，勾够2个自动在列表下方出现对比结果——比竞品两级弹窗更省一次点击，逻辑也更简单。**布局上吸取了月相面板的教训**：一开始又想当然地把这个面板放在工具栏原地展开（月相图标右边），实测发现只要历史列表有内容（尤其是展开对比结果后）面板会变得比月相面板高很多，跟右侧设置面板的重叠比之前更明显——这次没有再去猜一个新坐标了事，而是直接改成**屏幕居中的模态框**（`fixed inset-0` + 半透明遮罩，点遮罩关闭），从架构上避免跟任何侧边面板抢位置，这类"内容量不固定的列表+交互"本来就更适合用模态而不是贴边卡片。已完成，`tsc`/`lint`/`build` 全过，浏览器里点了两个不同地点验证历史正确写入 localStorage（地名/Bortle/SQM 都对得上），勾选两条记录后对比结果里的倍数（16.1x）跟手算的 `10^((20.37-17.35)/2.5)≈16.14` 精确吻合，控制台无报错。
- **顶部标题栏**：用户要加文案，参考竞品的排布方式。**关键发现**：竞品本身几乎没有传统营销文案（纯工具型单页应用，不能滚动），能参考的只有顶部一条"实体"标题横条（真正占布局空间，不是浮在地图上）。文案是我代笔起草的，不是抄竞品原文——竞品页面翻遍也就一行标题副标题加一条无关的 FAQ，没多少可抄的。新增 `src/components/SiteHeader.tsx`。**这次改动比想象的大**：要做出"真正占用空间的顶部横条"（不是叠加在地图上的浮层），就得把 `page.tsx` 的 `<main>` 从单纯 `h-screen` 改成 `flex flex-col`（header 用 `shrink-0`，剩余内容包一层 `relative flex-1` 容器），然后**把地图和其余七八个悬浮面板全部从 `fixed` 改成 `absolute`**（Map/SearchBar/Legend/MoonPhasePanel/LocationHistoryPanel 触发按钮/MapControls/LocationDetailPanel/DarkSkyPlacePanel）——这样它们的 `top-4`/`top-20` 才是相对于"内容区容器"（header 下方）而不是相对于整个视口，不用改任何一个具体的偏移数值，只是定位基准从 viewport 换成了 header 下面的那个 `relative` 容器。**LocationHistoryPanel 的对比弹窗保持 `fixed inset-0` 不变**——模态框应该盖住整个视口包括 header，不能只盖内容区。改的时候手滑了两次，把 `top-20` 顺手改成了 `top-4`（DarkSkyPlacePanel 和 MapControls 各错了一次）——这两个 `top-20` 本来就是特意用来避开同一水平线上的 SearchBar/缩放控件的，不是随便定的，改的时候只应该动 `fixed`→`absolute` 这一个词，不要连着数值一起动，每次改完随手 diff 一下比事后再排查快。

  **最初版本还带了一个"关于/常见问题"按钮 + 弹窗**（四条 FAQ：网站是做什么的/Bortle与SQM是什么/数据来自哪里/是不是官方产品，最后一条说明这是练习项目、参照竞品复刻、未获官方授权）。用户先要求删掉"是否官方产品"这一条，我提醒过"这个项目从功能到配色命名都是照竞品做的，上线后完全不提这层关系容易让人误会是官方产品或者有关联"，用户还是选择删——这是他自己的练习项目，最终由他决定内容取舍，我没有拒绝执行，只是说明了一次考虑。**后来用户干脆要求整个 FAQ 都删掉**，现在 `SiteHeader.tsx` 就是一个不带交互的服务端组件，只有标题+副标题，没有按钮也没有弹窗——如果以后又想加类似说明，直接参考这条记录里当时起草的文案内容（还留着上面这段），不用重新构思。已完成，`tsc`/`lint`/`build` 全过，浏览器里用 `getBoundingClientRect()` 实测了 header/缩放控件/SearchBar/月相按钮/历史按钮的坐标，确认互相之间和跟 56px 高的 header 都没有重叠；点地图测试了一次完整链路（点击查坐标→Bortle/SQM→地名→天气→曝光计算器全部正常），确认这次大范围的定位方式改动没有破坏任何现有功能。
- **中英文双语 + 右上角语言切换**：用真实 Chrome 核实过竞品的语言切换器——不在设置面板里，是顶部 header 最右侧一个"🇺🇸 English ▾"胶囊按钮，点开是个带搜索框的多语言下拉列表（旗帜图标+语言名+当前选中项打勾）。咱们只需要中英双语，做了简化版：同样的胶囊+下拉外观（旗帜+文字+箭头，下拉里两项+勾选态），去掉了竞品那个搜索框（只有两个选项用不上）。**架构**：`src/lib/i18n/language.ts`（`Language` 类型 + 语言元数据）+ `LanguageContext.tsx`（React Context，`useLanguage()` 提供 `language`/`setLanguage`/`t`）+ `translations.ts`（zh/en 两份完整字典，按组件分命名空间，参数化文案用函数而不是字符串拼接，比如 `t.darkSkyPlace.yearLabel(year)` 而不是模板字符串裸拼）。**所有 lib 文件里原本硬编码中文的 `Record<id, string>` 全部拆开**——`BORTLE_DESCRIPTIONS`（bortle.ts）、月相名称（moonPhase.ts）、WMO 天气码文字（weather.ts，emoji 留在原地，因为 emoji 不需要翻译）、暗空地点分类名（darkSkyPlaces.ts）、配色风格/底图/天气叠加/相机预设的 label（colorStyles.ts/basemapStyles.ts/weatherOverlay.ts/exposureCalculator.ts）——这些模块现在只留 id 和纯数据（颜色、URL、数值），文字挪进 `translations.ts` 按 id 查表，lib 层彻底跟语言无关。`stargazing.ts` 的评分函数原来直接返回拼好的中文 `constraint` 字符串（"主要受限于：云量较高"），改成只返回 `constraintFactor: "cloud"|"moon"|"light-pollution"|"none"` 这个 id，由 `WeatherSection.tsx` 在渲染时用 `t.weatherSection.constraint(factor)` 现场拼。**踩的坑**：语言状态要从 `localStorage` 读，最初写成 `useEffect(() => { const stored = localStorage.getItem(...); if (...) setLanguageState(stored); }, [])`，被 ESLint 的 `react-hooks/set-state-in-effect` 规则拦下来（这条规则之前在 `LocationHistoryPanel.tsx` 也踩过一次，见上面记录）——上次的解法是把读取挪到点击事件里，但这次是"页面一加载就要读"，没有对应的用户操作可以挪。**这次用的是更对症的方案**：改用 `useSyncExternalStore`（`subscribe`/`getSnapshot`/`getServerSnapshot` 三个函数），这个 hook 本来就是 React 官方为"从 React 外部的可变数据源读值"设计的，`getServerSnapshot` 返回默认语言给 SSR/首次渲染用，`getSnapshot` 在客户端读真实的 `localStorage` 值，两者不一致时 React 自己处理好了从"默认值"到"真实值"的切换，不需要手写 `setState`，也不会有 hydration 警告。**教训**：遇到"组件挂载时需要从外部存储同步一个值进 state"这种场景，先想 `useSyncExternalStore` 而不是 `useEffect+setState` 这个老套路——后者不仅会被新版 ESLint 规则拦，语义上也没有前者精确。已完成，`tsc`/`lint`/`build` 全过，`Claude_Browser` 里完整点了一遍：切到英文后 header/搜索框/图例/月相面板/位置历史面板/地图控制面板/位置详情面板/天气区块/曝光计算器全部正确显示英文，`aria-label` 也一起切了，控制台无报错；刷新页面语言保持不变（localStorage 生效）；`document.documentElement.lang` 也跟着切换到 `"en"`。
- **左下角 "i" Info/FAQ 弹窗（SEO 关键词落地文案）**：用户直接提供了一份成型的 SEO 文案方案（title/meta description + 一句话开场白 + 7 组 Q&A，按关键词类目组织），要求做成首页左下角一个 "i" 圆形按钮点开的弹窗，位置模仿竞品实际的 UI（竞品的语言切换器、"i" 信息按钮都是真实存在的 UI 元素，这次没有再重新去竞品站核实，直接照用户给的文案落地）。新增 `src/components/InfoPanel.tsx`（自包含开关状态的圆形触发按钮 + 局中弹窗，跟 `LocationHistoryPanel.tsx` 一样的 `fixed inset-0` 模态样式），文案进了 `translations.ts` 新增的 `infoPanel` 命名空间（`intro` + `qa: Array<{question, answer}>`）。**这次的文案是双语的**：用户明确要求内容跟着中英文切换走（问过 `AskUserQuestion`，用户选"中英文都要"而不是"只做英文"），英文用用户原文，中文是我另外写的等价关键词版本（波特尔等级/SQM天空亮度计/暗夜地点/观星地图/银河能见度/流星雨/极光概率这些中文长尾词，不是逐字直译）。**唯一改动的地方**：原文 Q7 提到"jump directly to a state page ... browse the full state and city list ... on our dedicated regional maps page"——这个"州级页面"和"地区列表页"目前项目里都没有做，原样搬上线等于对着搜索引擎和用户承诺一个不存在的功能。改成了"直接在搜索框里搜地名（Texas/California/NYC/Las Vegas 这些词还留着），地图会飞过去显示当地 Bortle/SQM 读数，专门的分地区列表页以后再做"——关键词一个没丢，但没有撒谎说这个功能已经存在。**布局**：小圆形按钮实测放在 Legend 面板右侧（`absolute bottom-4 left-64`），用 `getBoundingClientRect()` 量过，跟 Legend 面板（`x:16-240`）之间留了 16px 间隙，不重叠。同时把 `layout.tsx` 的 title/meta description 换成了用户给的 SEO 版本（"Light Pollution Map – Bortle Scale & Dark Sky Finder" / 那句压缩了核心关键词的 description）——这两个是 Next.js 的静态 `metadata`，服务端渲染一次给搜索引擎看，不随客户端语言切换变化，保留英文版（跟这批 SEO 关键词本来面向英文搜索一致）。**弹窗展现形式改过一版**：第一版做成了居中模态框（跟 `LocationHistoryPanel.tsx` 一样的 `fixed inset-0` + 半透明遮罩），用户发了竞品真实截图指出这不对——竞品的 FAQ 是**右侧贴边通栏面板**，没有遮罩，地图和左侧其他面板全程可见可操作，只有右侧一条竖条被面板盖住。改成 `absolute right-0 top-0 bottom-0 w-80`（贴内容区右边缘，占满 header 以下的整个高度），去掉黑色遮罩，加 `translate-x-full`↔`translate-x-0` 的 CSS transform 过渡做滑入滑出动画，面板本身一直挂载在 DOM 里（不是 `open &&` 条件渲染）保证动画有始末状态可以过渡。`z-30` 比 `MapControls`（`z-10`）高，打开时会盖住地图控制面板——这跟竞品行为一致，不是 bug。用 `getBoundingClientRect()` 实测过打开状态：面板 `x:960-1280`（贴右边缘）、`y:56-720`（56px 是 header 高度，往下占满到视口底部），没有盖住顶部 header。已完成，`tsc`/`lint`/`build` 全过，浏览器里验证过打开/关闭动画、控制台无报错；中文版内容还没有在浏览器里肉眼过一遍（下次回来对着中文界面点开验证一下文案有没有排版问题）。
- **Heading 结构补全 + canonical/OG URL 换成正式域名**：用户反馈"Headings结构都是空的"——查了一下，整个项目此前确实一个 `<h1>`-`<h6>` 标签都没有，所有面板标题全是 `<span>`/`<div>`，纯靠 className 做视觉层级，浏览器/搜索引擎看到的文档结构是平的。补了一版真实层级：`SiteHeader.tsx` 标题换成 `<h1>`（副标题换成 `<p>`），`Legend`/`MapControls`/`LocationDetailPanel`/`DarkSkyPlacePanel`/`MoonPhasePanel`/`LocationHistoryPanel`/`InfoPanel` 各自的面板标题换成 `<h2>`，`DarkSkyPlacePanel` 里具体地点名字、`LocationHistoryPanel` 的对比结果标题、`WeatherSection`/`ExposureCalculator` 的区块标题、`InfoPanel` 里每条 FAQ 问题换成 `<h3>`（FAQ 问题这次尤其重要——之前那批 SEO 关键词文案实际上就是塞在没有语义的 `<div>` 里，现在才是真正搜索引擎认的 heading 结构）。**没有全部转换**：`TrendChart.tsx` 的图表标题保持 `<div>` 不变，它更像图表的 caption 不是章节标题，硬套 heading 只会让层级变得杂乱。改 `<h1>`/`<h2>` 时特意确认过 Tailwind v4 的 Preflight 会把标题标签的 `font-size`/`font-weight`/`margin` 重置成 `inherit`/`0`（`globals.css` 里 `@import "tailwindcss"` 已经带这个），所以只是换标签名不用额外补样式，视觉不受影响，浏览器里实测过 `<h1>` 渲染出来和原来的 `<span>` 一模一样。同时用户确认了 `lightpollutionmap.io` 这个域名**已经买了、准备真实部署过去**——问过用户后确认不是占位，才把 `layout.tsx` 的 `metadataBase`/`alternates.canonical`/`openGraph.url` 三处一起配成这个域名（之前只有 title/description，没配 canonical/OG URL）。已完成，`tsc`/`lint`/`build` 全过，浏览器里通过 `document.querySelectorAll('h1,h2,h3...')` 实测过完整的标题层级（1 个 h1 + 若干 h2 + FAQ 的 7 个 h3，点开位置详情/月相面板后 h2/h3 数量也对得上），`canonical`/`og:url` 也实测确认指向了 `https://lightpollutionmap.io`。
- **中英文双语从纯客户端 Context 迁移到 next-intl（URL 路由级 i18n）**：用户指出一个关键 SEO 问题——之前的 `LanguageContext`（`useSyncExternalStore` 读 `localStorage`）是纯客户端状态，中英文切换不改变 URL，搜索引擎爬虫基本只会抓到默认语言那一版，中文版实际上不会被独立收录。用户明确要求换成 `next-intl` 做 URL 路由级方案（问过是"轻量 URL 路由"还是"完整迁移 next-intl"，用户选完整迁移）。

  **路由结构**：`src/i18n/routing.ts` 定义 `locales:["en","zh"]`、`defaultLocale:"en"`、`localePrefix:"as-needed"`——默认语言（英文）不带前缀直接在根路径 `/`，非默认语言带前缀 `/zh`，这样根路径既是英文版首页，也是 canonical 首选（对 SEO 友好），中文版是独立可收录的 `/zh` URL。`src/i18n/navigation.ts`（`createNavigation` 导出 locale-aware 的 `Link`/`usePathname`/`useRouter`）+ `src/i18n/request.ts`（`getRequestConfig`，按 locale 动态 `import` 对应的 messages json）+ `src/middleware.ts`（`createMiddleware(routing)`，负责首次访问时的 `Accept-Language` 协商重定向和后续访问的 locale 识别）。**`next.config.ts`** 用 `createNextIntlPlugin("./src/i18n/request.ts")` 包一层。**目录结构**：`src/app/layout.tsx`/`page.tsx` 整个挪成 `src/app/[locale]/layout.tsx`/`page.tsx`——`<html lang>` 必须在能拿到 `params.locale` 的地方渲染，所以不能再有独立的根 `app/layout.tsx`（会变成嵌套 `<html>`，不合法），`[locale]/layout.tsx` 就是唯一的顶层 HTML 外壳，`generateStaticParams` 返回两个 locale 让 `/en`/`/zh` 都在构建时静态生成（`npm run build` 输出确认 `● /[locale] ├ /en ├ /zh` 都是 SSG，不是运行时动态渲染——这才是"每个语言版本都是独立可抓取静态页面"这个 SEO 目标真正落地的地方）。

  **文案从 `src/lib/i18n/translations.ts`（TS 对象 + 函数）搬到 `messages/en.json` / `messages/zh.json`（ICU MessageFormat）**：参数化文案从"调用函数"改成"`{占位符}` 语法"，比如 `legend.title(styleLabel)` 变成 `"Light Pollution Intensity (VIIRS satellite data · {styleLabel})"`，组件里改成 `t("title", {styleLabel})`。**`stargazing.ts` 的 `constraint(factor)` 原来是一个函数根据 4 种 factor 拼不同句子**（"none" 时是完全不同的句子结构，不只是替换一个词）——ICU 的 `select` 语法能做到但连字符 key（`light-pollution`）在 select 语法里不保险，改成更直接的方案：4 条独立的静态 message（`constraintCloud`/`constraintMoon`/`constraintLightPollution`/`constraintNone`），组件里用一个 `Record<ConstraintFactor, string>` 把 factor id 映射到 message key 名，再 `t(key)`——比硬凑 ICU select 语法更不容易出错。**FAQ 的 7 组问答**（`infoPanel.qa`）是结构化数组，不是"一句待格式化的文案"，用 `t.raw("qa")` 直接拿原始 JSON（不经过 ICU 格式化，适合这种整段富内容）。**数字/id 做 key 的字典**（`dataLabels.bortleDescriptions`、`weatherCodes`、各种 `Record<id,label>`）用嵌套 namespace + 动态字符串 key 查（比如 `useTranslations("dataLabels.bortleDescriptions")` 然后 `t(String(bortleClass))`），JSON 里数字键写成字符串（`"1": "..."`）完全没问题。

  **踩的坑**：
  1. **ICU 转义英文撇号 `'` 结果显示成了两个 `''`**——一开始以为 ICU MessageFormat 要求所有文本里的裸单引号都得转义成 `''`（这是标准 ICU 规范），把 FAQ 答案里 6 处 `it's`/`area's` 之类的撇号全转成了 `''`，结果浏览器里渲染出来真的是两个连续单引号，很难看。**根因**：next-intl（用的底层 formatjs/intl-messageformat）对**不含任何 `{参数}` 占位符的纯静态消息**，实际上不会跑完整的 ICU 解析器，直接原样返回字符串——这种消息里的撇号根本不需要转义，转义反而是在瞎改。**教训**：ICU 转义规则要看这条消息实际有没有走 ICU 解析路径（有没有 `{}` 占位符），不能无脑套用"ICU 文档说要转义"就全文批量转义，先写一条真实内容测一下再决定要不要转义，比套规则更可靠——事后用 `sed` 把 6 处 `''` 挑出来批量改回单个 `'` 解决的。
  2. **TypeScript 严格类型化的消息 key（`AppConfig.Messages` 全局声明）跟这个项目大量"动态 key 查字典"的用法直接冲突**——next-intl 官方推荐加一个 `global.d.ts` 声明 `Messages` 类型，让 `t("some.key")` 在编译期就能检查 key 拼错。试着加了一下，发现项目里太多地方是"运行时才知道具体 key"（`t(String(bortleClass))`、`t(`${styleId}.label`)`、`t(`weatherCodes.${code}`)` 这类），这些动态拼出来的 `string` 类型跟严格类型要求的字面量 key 联合类型对不上，会产生一堆类型错误，要么到处加类型断言、要么放弃这个类型安全特性。**决定放弃 `global.d.ts`**——保持 `t()` 是宽松的 `(key: string) => string`，跟这个项目原来 `Record<string,string>` 式的用法习惯一致，用编译期 key 类型安全换来的收益，抵不过到处加断言的代码噪音。
  3. **`useSearchParams()`（语言切换要保留地图 lat/lng/zoom）导致 `/[locale]` 构建时静态生成失败**——`SiteHeader.tsx` 的语言切换器要把当前 URL 的查询参数原样带到新 locale 的 URL 上（不然切语言会把地图视角重置成默认位置），用了 `next/navigation` 的 `useSearchParams()`。Next.js 要求任何用到它的组件必须包在 `<Suspense>` 里，否则整个页面在静态生成阶段被强制退化成纯客户端渲染，报 `useSearchParams() should be wrapped in a suspense boundary` 直接构建失败。**修法**：只把 `<LanguageSwitcher />` 包一层 `<Suspense fallback={...}>`，不用动其他组件。
  4. **`middleware.ts` 文件命名约定在 Next.js 16 里标了 deprecated**（构建时有警告，建议改名 `proxy.ts`），因为不是构建阻断错误、`next-intl` 当前版本文档也还是用 `middleware.ts` 举例，这次先不追这个警告，留着以后 next-intl 或者 Next.js 文档更新后再一起改。
  5. **验证阶段撞上了这个会话一直存在的已知坑**（见下方"已知坑点"）：`Claude_Browser` 的 `document.hidden` 是 `true`，导致 MapLibre 渲染/点击响应挂起，正常点击地图选点没反应。用的是已经记录过的应对方式——临时在 `Map.tsx` 挂一行 `window.__map = map`（验证完立刻删掉），在浏览器 JS 里手动 `map.fire('click', {lngLat, point})` 触发一次真实点击，绕开卡住的原生指针事件路径，验证完 `LocationDetailPanel`/`WeatherSection`/`ExposureCalculator` 这些依赖点击才会渲染的动态查表内容（Bortle 描述、天气码、评分等级、相机预设）全部正确。

  **验证**：`tsc`/`lint`/`build` 全过（`build` 输出确认 `/en`/`/zh` 都是 SSG 静态页）。浏览器里：`/zh` 中文内容完整渲染、控制台无报错；语言切换器从 `/zh?lat=..&lng=..&zoom=..` 切到英文后正确落到 `/?lat=..&lng=..&zoom=..`（地图状态保留，`/zh` 前缀被去掉）；`<html lang>`、`<link rel="canonical">`、`<link rel="alternate" hreflang="en/zh">` 全部实测对应正确的 URL；FAQ 的英文撇号问题修复后复核过不再显示 `''`；用 `window.__map` 触发的一次点击验证了 Location Details/Nighttime Weather/Exposure Calculator 里全部动态查表内容（波特尔描述、天气图标文字、评分等级 Poor/Marginal/Fair/Good、"Main limiting factor" 系列、相机预设名）都正确显示英文，无 `MISSING_MESSAGE` 之类的报错。`src/lib/i18n/LanguageContext.tsx`、`src/lib/i18n/translations.ts` 已删除，`src/lib/i18n/language.ts` 精简成只剩语言切换器要用的 `LANGUAGE_META`（旗帜+名称，这个不算翻译内容，跟 UI chrome 绑在一起更合理，没有搬进 messages json）。
- **日出日落黄金时刻计算器（独立页面 `/golden-hour`）**：用户说"继续之后的功能增加"但没指定具体做什么，去竞品站 Resources 下拉菜单里又找了一遍——之前已经明确跳过的（Clear-night climate 没免费数据源、SQM 分析照片复杂度太高）之外，发现了三个没做过的真实功能：Golden Hour & Light Direction（独立页面，纯天文计算）、Global Light Pollution Rankings（244 个国家/地区的 SQM 排行榜，需要 Earth Engine 按国界做 zonal statistics，工作量大不少）、Search Best Stargazing Spots（没细看，可能跟已有的暗空地点图层/历史对比功能重叠）。列出来问用户选哪个，选了黄金时刻计算器。

  **复用 `terminator.ts` 现成的太阳位置数学**：把 `dayOfYear`/`solarDeclinationDeg`/`equationOfTimeMinutes`/`subsolarLongitudeDeg` 四个原本私有的函数导出，新增 `src/lib/sunPosition.ts` 在此基础上加两块：①标准球面三角公式算某一时刻任意经纬度的太阳仰角+方位角（`computeSolarPosition`）；②**解析解**（不是采样搜索）反过来算"仰角到达某个目标值时的时角"（`solveElevationCrossing`，`cosH = (sin(目标仰角) - sin(lat)sin(赤纬)) / (cos(lat)cos(赤纬))`，解出的时角分别代入上午/下午两个分支），用这个直接求出日出日落（仰角 -0.833°，标准大气折射修正值）、黄金时刻边界（-4°~+6°）、蓝调时刻边界（-6°~-4°）对应的精确时刻和太阳方位角——不需要每隔几分钟采样一次再插值，跟这个项目一贯"能解析解就不用近似"的风格一致。

  **验证异常扎实**：写了个独立 Node 脚本，用真实伦敦 2026-08-10 的数据（这天正好是竞品页面截图时显示的日期）反推验证——日出/日落/晨昏蓝调黄金时刻四组时间点，全部对上（时间误差在 1 分钟以内，方位角误差在 1° 以内），比这个项目里之前几次"近似公式"的验证结果都更精确（这次用的是精确解析解，不是近似）。之后用同一份代码在真实浏览器里选伦敦、东京两个地点交叉核对，数字跟独立脚本算的完全一致。

  **时区处理是唯一做了简化的地方**：真实时区要按政治边界+夏令时来定，没有引入额外的时区数据库依赖（保持"轻量、不过度设计"）。分两种情况处理——用"使用我的位置"（真实 `navigator.geolocation`）时，直接用浏览器自己的 `Intl`/`toLocaleTimeString` 本地时区格式化，因为设备时区基本就等于用户实际所在地的时区，准确；手动搜索任意地点时，退化成经度粗略换算 UTC 偏移（`Math.round(lng/15)`），明确在 UI 里用小字标注这是近似值、可能在时区或夏令时边界附近差一小时——这个简化点写清楚了，没有假装精确。

  **新增文件**：`src/lib/sunPosition.ts`（计算核心）、`src/components/GoldenHourCompass.tsx`（SVG 罗盘图，N/E/S/W 标注 + 晨昏黄金/蓝调时刻方位角弧线，方位角 0°=正上方=正北，顺时针递增，跟标准罗盘方向一致）、`src/app/[locale]/golden-hour/page.tsx`（独立页面，搜索/定位 + 日期选择 + 结果卡片 + 罗盘图）、`src/app/[locale]/golden-hour/layout.tsx`（专门加这一层是因为 page.tsx 本身是 Client Component 不能导出 `generateMetadata`，用一个薄的 Server Component layout 包一层来提供这个页面自己的 `<title>`/description，不跟主页共用）。**`SiteHeader.tsx` 加了个 `titleAsHeading` 参数**：主页没有其他候选 h1，站点标题理应是 `<h1>`；但黄金时刻页面自己已经有真正的 `<h1>`（"Golden Hour & Light Direction"），两个 h1 挤在一个页面里不是好的文档结构，所以在非主页场景传 `titleAsHeading={false}` 把它降级成普通 `<span>`——跟不久前刚补的 heading 层级工作保持一致，没有做完就忘。

  **验证方式**：`tsc`/`lint`/`build` 全过（build 输出确认 `/en/golden-hour`、`/zh/golden-hour` 两个都是 SSG 静态页，符合一贯的"每个语言独立可收录 URL"目标）。浏览器里中英文两个语言、搜索地点+"使用我的位置"两种定位方式、罗盘 SVG 渲染都测过；中途不小心在 dev server 还在跑的时候删了 `.next` 把它搞崩过一次，重启后干净验证了一遍——遇到过一条一直在控制台里挂着的 500 报错，一开始担心是真 bug，最后用 `curl` 直接查了三个路由的真实 HTTP 状态码全部是干净的 200（连带页面 `<title>` 一起用 `grep` 核对过），确认这条控制台报错是这个会话 Browser pane 本身的环境噪音（跟 WebSocket HMR 连接失败一起出现，开发时的热更新连接问题，不影响页面本身，生产环境也用不到这个连接），不是真正的应用 bug——遇到这种"控制台有报错但看不出关联哪个资源"的情况，直接拿 curl 查真实响应码比在浏览器里反复排查更快更可靠。
- **搜索附近最佳观星地点（Search Best Stargazing Spots）**：去竞品站点了这个按钮才发现具体做法——弹出一个设置框（搜索半径/最多结果数/地点间最小距离三个滑块 + 排除海洋/湖泊/仅本国/智能排序四个开关），点"开始搜索"后地图上出现编号 1-10 的标记，右侧出现带距离/SQM/评分的结果列表。**明确跳过的部分**：排除海洋/排除湖泊/仅本国/智能排序四个开关——前两个需要引入额外的陆地/水体掩膜数据集，后两个具体逻辑是竞品私有黑盒，跟 `bortle.ts`/`stargazing.ts` 一直以来的取舍原则一样，不追求照抄一个没法验证对不对的黑盒实现，只做核心的"搜索半径+结果数+最小间距"三个滑块。

  **核心算法自己设计的，不是抄的**：`src/lib/bestSpots.ts` 纯函数——①`generateSearchGrid` 在圆形搜索范围内生成候选点网格，网格间距故意跟半径成正比（`spacing = radius/13`），这样候选点数量无论半径多大都稳定在几百个左右（不然 300km 半径细网格会生成几万个点，直接把批量查询拖垮）；②`haversineDistanceKm` 标准球面距离公式；③`computeSpotScore` 明确标注是简化近似评分（SQM 归一化的"暗度分" 减去 "离中心越远扣分越多"的距离惩罚，不是逆向工程出竞品的私有排序算法）；④`selectSpacedTopSpots` 贪心算法，按分数从高到低遍历，只收不跟已选点太近的候选，直到凑够结果数——避免最佳结果全部挤在同一小片暗区。**验证方式比较硬核**：写了独立 Node 脚本用已知真实距离核对 `haversineDistanceKm`（1° 纬度=111.19km，洛杉矶到旧金山=559.1km，跟公开数据完全对上），又验证了网格候选点数量在 10-300km 半径范围内确实稳定在 300-520 个之间。

  **后端**：`src/app/api/best-spots/route.ts`，用 Earth Engine 的 `image.sampleRegions()`（不是逐点循环调用 `reduceRegion`）一次性批量采样几百个候选点的 VIIRS 辐射值——这是这个项目第一次用 EE 的批量点采样接口，之前的 `/api/point-value` 一直是单点查询。**真实测过，不是纸上谈兵**：直接 `curl` 打了真实坐标（洛杉矶，半径 100km），3.3 秒内拿到 10 个真实排序结果，又写脚本验证了返回结果两两之间的最小间距确实 ≥ minDistanceKm 参数、评分确实是降序排列——约束条件真的生效了，不是摆设。

  **前端**：`BestSpotsPanel.tsx`（触发按钮+设置弹窗，接进 `LocationDetailPanel.tsx` 最下面，紧跟在曝光计算器后面）+ `BestSpotsResults.tsx`（右侧结果列表，浮层定位在 `right-72`，用 `getBoundingClientRect()` 实测过跟 `MapControls`（`right-4`）之间留了干净的 16px 间隙，不重叠）+ `Map.tsx` 新增一个 GeoJSON circle+symbol 图层显示编号标记（复用 `ensureXxxLayer`/点击命中检测的既有模式，跟暗空地点图层的写法完全一致）。点结果列表里的某一条，或者点地图上的编号标记，都会调用同一个"选中这个点"的回调，触发 `LocationDetailPanel` 重新挂载显示该点详情——这时候点位切换会用新的经纬度重新弹出搜索按钮，可以从新地点再发起一轮搜索。

  **测试时误判过一次"bug"，教训值得记一下**：浏览器里第二次点击结果列表某一项后，读到的按钮文字是"139.5 km"，一度以为是搜索半径没生效（100km 范围内不该出现 139.5km 的结果）。查了半天发现这是我自己读取 `textContent` 时的假象——结果按钮内部结构是"排名徽标（比如数字 1）"紧跟着"距离文字（39.5 km）"，两段文字之间没有分隔符，`textContent` 读出来是"1"和"39.5 km"直接拼接成"139.5 km"，完全是我自己解析错了，不是真的产生了 139.5 公里那个数字。**教训**：用 `textContent` 读包含图标/徽标的复合按钮内容时，相邻文本节点会无缝拼接，容易把"标号"看成数值的一部分——遇到看起来异常的数字，先去确认一下这数字是不是原本就该独立存在的文本节点，不要急着当成计算逻辑的 bug 去排查。

  已完成，`tsc`/`lint`/`build` 全过（`build` 确认 `/api/best-spots` 是新增的动态路由）。浏览器里中英文各完整跑了一遍：设置弹窗 → 开始搜索 → 结果列表 + 地图编号标记同时出现 → 点结果列表任意一项 → `LocationDetailPanel` 正确切到新坐标 → 从新坐标再发起一轮搜索、结果列表正确刷新成以新坐标为中心的新排名（用 `read_network_requests` 核对过请求参数里的 lat/lng 确实换成了新坐标，不是用的旧值）——控制台除了这个会话一直存在的环境噪音（跟 `document.hidden` 卡死相关的已知坑，用 `window.__map` 手动触发点击事件绕过验证）之外没有真正的报错。
- **全球光污染排行榜（独立页面 `/rankings`）**：竞品 Resources 菜单里排到最后一个还没做的功能——按国家/地区列出平均 SQM、天空亮度倍数、Bortle ≤4 占比的排行表。跟前面几个"纯前端计算"或"实时请求外部 API"的功能都不一样，这次要用 Earth Engine 按 312 个国家/地区的真实国界做 zonal statistics（`reduceRegions`），实测下来一次全量请求（哪怕加大 `scale`）光是俄罗斯/加拿大/中国/澳大利亚/巴西这几个大国就得连续跑几十秒到几分钟，**架构上直接排除了"实时接口"或"哪怕是带长缓存的 revalidate 路由"这两个选项**——用户第一次访问如果缓存是空的，最坏情况会撞上这几分钟的冷启动，体验上不可接受。改成跟 `darkSkyPlaces.json` 一模一样的套路：**离线一次性算好存成静态 JSON**（`src/data/countryLightPollution.json`，281 个国家/地区，构建时直接 `import` 进 `src/lib/countryRankings.ts`，生产环境这个功能全程零 Earth Engine 调用）。

  **国界数据集选型**：对比了 `USDOS/LSIB_SIMPLE/2017`（312 个 feature，字段是干净的两字母代码 `country_co`）和 `FAO/GAUL/2015/level0`（292 个 feature，`ADM0_CODE` 是数字流水号不好认），选了前者。**LSIB_SIMPLE 把跨海/多块陆地的国家拆成好几个同名 feature**（实测 "Canada" 有 8 条、"Russia" 有 5 条），必须在拿到 `reduceRegions` 原始结果后按 `pixelCount` 加权平均合并成一行，不能直接拿原始 feature 数当结果条数。**南极洲会让 `reduceRegions` 直接报"无法从 EPSG:4326 PLANAR 转换"的坐标转换错误**（极点附近的几何形状是已知问题，不管 `scale` 调多大都一样），而且南极本来也没有常住人口意义上的"国家光污染水平"可言，直接 `.filter(ee.Filter.neq("country_na", "Antarctica"))` 排除，语义上也说得过去。

  **逐像素 SQM 换算复用了 `bortle.ts` 自己的公式**（`22.0 - log10(max(辐射值,0)+1) * 2.0`，`clamp(16,22)`），用 EE 的 `image.expression()` 现算成一个 SQM 波段，再算一个 `sqm >= 20.8`（`bortle.ts` 里 Bortle 4 级的 SQM 门槛）的布尔波段，两个波段一起 `reduceRegions(mean+count)`——直接对 SQM 波段取平均，而不是先平均辐射值再换算成 SQM，因为辐射值本身跨好几个数量级、换算公式又是对数压缩的，先平均辐射值会被城市里少数几个极亮像素带偏均值，顺序不能换。**排行榜"倍数"这一列直接复用了历史对比功能已有的 `computeSkyBackgroundRatio(darkestSqm, sqm)`**（`locationHistory.ts`，星等制标准公式），没有重新发明一遍。

  **前端**：`src/app/[locale]/rankings/page.tsx`（可搜索+可排序表格，纯客户端 `useMemo` 过滤排序，不需要接口，跟数据量只有 281 条完全对得上量级）+ `layout.tsx`（跟黄金时刻页一样，薄 Server Component 单独提供这个页面的 `generateMetadata`）+ `SiteHeader.tsx` 加了跟"黄金时刻"同款样式的"🌍 Rankings"入口链接。

  **验证结果比预期更能自证数据靠谱**：281 条排行榜结果里，最亮（光污染最重）的几个几乎精确对上真实世界常识——梵蒂冈、新加坡、澳门、巴林、香港、马耳他、摩纳哥排在光污染最重的前列，最暗的是所罗门群岛、基里巴斯、图瓦卢这类偏远岛国——不是刻意调出来的，是真实 VIIRS 数据 + 简化公式自然算出来的结果，侧面印证了这套简化方法论（跟 `bortle.ts`/`bestSpots.ts` 一直用的"明确写清楚是近似值，但方向要对"原则一致）没有走偏。

  **离线采集踩的坑**：全量 312 国分 17 批（每批 20 个原始 feature）跑，其中一批（offset=40，包含 Burundi 到 China，里面混了 Canada/Chile/China 三个大国）在 `--max-time 300` 下超时失败，但拆成两个各 10 个 feature 的子批次分别都在一分钟内跑完——说明不是某个特定国家的几何问题（跟南极洲那种"任何 scale 都会报错"的硬故障不一样），单纯是"一次请求里堆的大国太多，Earth Engine 服务端计算量叠加导致耗时逼近或超过超时阈值"这种资源类瞬时问题，重试了一次同样的整批请求（这次连接已经"热"了）就正常在 4 分半内跑完，不需要真的调整批次划分逻辑。**教训**：批量请求某一批偶发超时/失败时，先用二分法拆成更小的子批次分别测试定位是不是某个特定条目的硬故障，如果拆开都能跑通，大概率是资源竞争或冷启动这种瞬时问题，原样重试往往就好了，不用急着永久调小批次大小或加复杂的重试退避逻辑。
- **顶部图标工具栏（把暗空地点/极光/昼夜分界线三个开关、黄金时刻、最佳观星地点、月相都收进同一排图标）**：用户直接点名要求把"认证暗空地点"/"极光概率（NOAA 预测）"/"昼夜分界线"这三个原本只在右侧 `MapControls` 设置面板里的开关，做成图标放到月相按钮所在的位置，鼠标悬停显示说明；接着追加"黄金时刻也放在月相前"，再追加"最佳观星地点也要"——最终变成一排 6 个图标（黄金时刻→暗空地点→极光→昼夜分界线→最佳观星地点→月相）。**没有删除原来 `MapControls` 里的三个开关**——用户没要求移除，两处开关绑定同一个 `page.tsx` state，互相同步，纯粹是加一条更快的入口，不是替换。

  **新增 `src/components/IconToolbar.tsx`** 作为唯一的顶层容器，挂载在 `MoonPhasePanel` 原来单独占用的位置（`absolute left-1/2 top-4`）。内部 `ToolbarIconButton` 是个小的本地组件：图标本体 + `group relative` 包裹 + 一个 `absolute top-full opacity-0 group-hover:opacity-100` 的纯 CSS 提示条（不需要 JS 控制显隐，鼠标移开自动消失），三个开关按钮的提示文字直接复用 `mapControls.darkSkyPlacesLabel`/`auroraLabel`/`terminatorLabel`（用户打字问的文案本来就是照抄这三个已有翻译 key 的原文），黄金时刻复用 `goldenHour.navLabel`，最佳观星地点复用 `bestSpots.triggerLabel`——一个新翻译 key 都没加。**`MoonPhasePanel.tsx` 做了结构性改动**：原来是自己独立 `absolute left-1/2 top-4` 定位（开关按钮和展开面板共用同一个定位），现在改成普通 `relative` 容器塞进 `IconToolbar` 的 flex 行里，展开的下拉卡片改成相对自己按钮 `absolute top-full mt-2` 定位——这样月相图标能跟其他 5 个图标平铺在一行，展开时卡片挂在自己按钮正下方，不会把整排图标顶飞。

  **最佳观星地点这次做成了"图标一点就搜"的快捷入口，用的是地图当前中心点，不是选中的位置**——原来的入口在 `LocationDetailPanel` 里（先点地图选一个点，再点面板底部的"🔭"按钮，围绕这个点搜索），工具栏图标场景没有"先选点"这一步，围绕地图中心点搜索更符合直觉。为了不重复一遍搜索弹窗的滑块和请求逻辑，把原来揉在 `BestSpotsPanel.tsx` 里的弹窗抽成独立的 `src/components/BestSpotsSearchModal.tsx`（半径/结果数/最小间距三个滑块+搜索请求，纯受控组件，`lat`/`lng`/`onResults`/`onClose` 四个 props），`BestSpotsPanel.tsx`（`LocationDetailPanel` 里那个入口）和新的工具栏图标各自只负责"点了之后从哪拿 lat/lng、状态放哪"，弹窗内部逻辑只写一份。**`Map.tsx` 的 `MapHandle` 加了个 `getCenter()`**（包一层 `mapRef.current.getCenter()`），工具栏图标点击时调用它拿当前地图中心坐标传给搜索弹窗。

  **验证方式踩了这个会话一直存在的老坑，而且这次发现它比之前记录的范围更广**：这次 `Claude_Browser` 里 `document.hidden` 还是 `true`（老问题），但这次新发现的是——**不只是 MapLibre 的渲染循环被卡住，连 React 自己的调度也被拖慢了**：用 `computer` 工具真实点击一个开关图标后立刻查 `aria-pressed`，读到的还是点击前的旧值；紧接着用 `.click()` 直接调用原生 DOM API 触发，立刺再查也是旧值；等了两秒后再查，才变成点击后应有的新值——说明点击事件本身和 React 的 `setState` 都正常触发了，只是 `document.hidden=true` 让 React 调度器（用 `MessageChannel`/`requestIdleCallback` 兜底，浏览器对隐藏标签页天然会降低这类调度优先级）把这次更新的渲染推迟了，不是真的卡死，等一下就好。**另外发现 `mcp__Claude_Browser__computer` 的 `hover` 动作在这个环境下完全不会触发真实 CSS `:hover` 伪类**（`element.matches(':hover')` 手动查证实是 `false`）——`group-hover` 这种纯 CSS 提示条因此没法用鼠标模拟悬停来验证效果，改成直接给提示条元素临时塞一个 `style.opacity=1` 强制显形，截图确认文字/位置/样式都对，再把内联样式撤掉，绕开了"没法真悬停"这个环境限制去验证一个纯 CSS 效果。**还有个纯属虚惊的插曲**：`get_page_text` 读到的正文里一直混着"光污染地图常见问题"这段 FAQ 文字，一度以为是点击误触了 FAQ 弹窗，查了半天才想起来 `InfoPanel.tsx` 从很早的版本开始就是**全程挂载在 DOM 里、靠 CSS `translate-x-full` 滑出滑入**而不是条件渲染（当时是为了让滑入滑出动画有头有尾），`get_page_text` 不看 CSS 可见性、只抓文本，自然会把这块本来就一直在 DOM 里、只是视觉上滑到屏幕外的内容也读出来——不是这次改动引入的新问题，是这类基于 CSS 动画常驻挂载的组件本来就会让纯文本抓取工具"看见"视觉上不可见的内容，以后遇到类似"文本输出里出现了没点开的面板内容"不用怀疑是真的意外打开了，先想到这一层。

  已完成，`tsc`/`lint`/生产 `build` 全过（`build` 输出确认 6 个页面路由都还是正常的 SSG/动态路由，没有因为这次改动受影响）。中英文两个语言都用 `read_page`/DOM 查询确认了 6 个图标的 `aria-label` 顺序和文案正确（黄金时刻→认证暗空地点/Certified Dark Sky Places→极光概率/Aurora probability→昼夜分界线/Day-night terminator→搜索附近最佳观星地点/Search Best Stargazing Spots Nearby→月相），点击验证了暗空地点开关的 `aria-pressed`/背景色正确翻转、且跟 `MapControls` 里对应开关的 `aria-checked` 保持同步，月相图标点击后下拉卡片内容和定位都正确，最佳观星地点图标点击后搜索弹窗正确弹出。
- **图标工具栏第二轮调整：重新排序 + 加入历史记录 + 改成靠右上 + 黄金时刻从跳转整页改成弹窗 + 删掉顶部导航里的黄金时刻链接**：用户给了明确的最终顺序（认证暗空地点→极光概率→昼夜分界线→观星地点→月相→黄金时刻→历史记录，黄金时刻从"排第一"改成了排在月相后面），并且提了三个新要求：①整排图标从水平居中改成右上角对齐；②"最上面那个黄金时刻"（`SiteHeader.tsx` 里原来那个跟 Rankings 并列的"☀️ Golden Hour"顶部导航链接，跟工具栏里新加的图标是两个不同的入口）删掉；③黄金时刻原来点了是整页跳转到 `/golden-hour`，改成跟月相/历史记录一样是个不覆盖全屏的小弹窗。

  **`IconToolbar.tsx` 容器从 `absolute left-1/2 top-4 -translate-x-1/2`（水平居中）改成 `absolute right-4 top-4`**（贴右上角，跟下方 `MapControls` 设置面板同一条竖线对齐，`getBoundingClientRect()` 实测两者间有约 26px 干净间隙不重叠）。**所有下拉卡片/提示条的对齐方式从"以自己按钮为中心" `left-1/2 -translate-x-1/2` 全部改成"贴自己按钮右边缘" `right-0`**（`ToolbarIconButton` 的提示条、`MoonPhasePanel`、`LocationHistoryPanel` 的展开内容三处一起改）——这是右上角布局必须配套的改动：整排图标现在贴着视口右边缘，如果下拉内容还按旧的居中方式展开，最右边几个图标的卡片会有一半探出视口外side被裁切；改成向左展开（`right-0`，无 `translate`）之后，不管图标在这排的第几个位置，展开内容永远贴着自己按钮的右边缘向左延伸，不会超出右边界。

  **`LocationHistoryPanel.tsx` 结构性改动跟之前 `MoonPhasePanel.tsx` 改造是同一个模式**：原来是 `if (!open) return <button.../>` 这种"关闭时只渲染按钮、打开时只渲染弹窗"的二选一结构，触发按钮自己 `absolute left-1/2 top-4 translate-x-6` 硬编码在旁边——放进 flex 排列的工具栏后，这种结构会导致弹窗打开时按钮从 DOM 里消失，右边其余图标（这次没有其他图标了，但作为通用模式仍要保持一致）会跟着往左挤一格。改成跟 `MoonPhasePanel.tsx` 一样"按钮永远渲染，弹窗作为条件渲染的兄弟节点叠在上面"，弹窗内容本身还是保留原来 `fixed inset-0` 的全屏遮罩居中模态（这个没有被要求改成非全屏的，只有黄金时刻被点名）。

  **黄金时刻从"整页 `Link`"改成"内嵌弹窗"，为了不写两份计算器 UI，把原来 `src/app/[locale]/golden-hour/page.tsx` 里除页面外壳（`SiteHeader`/返回地图链接/`<h1>`/副标题）之外的全部内容（定位输入、日期选择、罗盘、日出日落卡片、四个时段窗口、"什么是黄金时刻"说明）抽成新文件 `src/components/GoldenHourCalculator.tsx`**（纯逻辑不变，只是把 JSX 从 page 里搬出来），`/golden-hour` 页面本身瘦身成外壳 + `<GoldenHourCalculator />`，路由本身**没有删除**——继续保留作为独立可收录 SEO 页面和分享链接，只是顶部导航不再直接链过去。`IconToolbar.tsx` 里新增一个本地 `GoldenHourButton` 组件，点击后展开一个 `w-[min(90vw,32rem)] max-h-[80vh] overflow-y-auto` 的浮层卡片（宽度按 512px 定的，够放下 200×200 的罗盘 SVG 加两列日出日落卡片并排；`max-h-[80vh]` + 自身滚动条封顶，保证内容再长也不会把弹窗撑到盖住全屏），里面渲染同一个 `<GoldenHourCalculator />`——跟页面版本内容和交互完全一致，只是外面套了个有边框、贴自己按钮右边缘展开的小卡片，不是撑满视口的模态。**新增了一个之前没有的翻译 key** `goldenHour.closeAria`（en/zh 都补了）——原来的整页版本没有关闭按钮（离开页面直接靠"返回地图"链接），弹窗版本需要一个 ✕ 关闭按钮，之前没这个文案。

  **验证阶段又踩到一次跟上次一样的假警报**：`preview_logs` 里翻出好几条 `ReferenceError: LocationHistoryPanel is not defined` / `ReferenceError: tGoldenHour is not defined`，一开始以为改漏了什么引用，仔细看时间戳和调用栈才确认是 HMR 热更新过程中的历史快照（改 `page.tsx`/`SiteHeader.tsx` 引用关系的那几次中间状态，编辑器落盘和 webpack 重新编译之间有个短暂窗口），紧接着查最新几条日志全是干净的 200，也用 JS 直接查询当前真实 DOM（按钮顺序、`aria-label`、弹窗内容、`getBoundingClientRect()`）反复确认过没问题——**这类"热更新中间态残留在日志里"的假警报这个项目已经踩过不止一次，往后看到 `preview_logs`/`read_console_messages` 里报错，第一反应应该是先看时间戳新旧、再用当前 DOM 状态交叉验证，而不是直接假设代码有问题去改**。

  已完成，`tsc`/`lint`/生产 `build`（清过 `.next` 后重新 `dev` 启动，避开"build 产物和 dev 模式缓存混用"那个已知坑）全过，中英文两个语言都用 DOM 查询确认了 7 个图标最终顺序（认证暗空地点→极光概率→昼夜分界线→搜索附近最佳观星地点→月相→黄金时刻→历史记录）、整排贴右上角（`right-4` 精确到 16px 误差为 0）、黄金时刻弹窗尺寸/位置（512×273px 起，随内容增长但不会超过视口，验证时截图确认远小于整个 1280×800 视口）、顶部导航"☀️ Golden Hour"链接已移除（只剩"🌍 Rankings"）、`/golden-hour` 独立页面本身仍然可以正常访问（SSG 输出没变化）。
- **图标工具栏第三轮调整：把 `MapControls` 设置面板本身也并进同一排，排在最后**：用户发了张截图（历史记录图标和一个齿轮图标并排显示）+ 一句话"这个设置放在历史记录后面，并排"，指的是原来单独浮在 `right-4 top-20` 的 `MapControls` 齿轮触发按钮——这是这个项目最早期就有的设置面板（基图/配色风格/透明度等一堆选项），之前几轮工具栏改造一直没动它，这次要求它也变成同一排图标里的最后一个。

  **`MapControls.tsx` 做了跟之前 `LocationHistoryPanel.tsx`/`MoonPhasePanel.tsx` 完全同一套模式的结构性改造**：原来是 `if (!open) return <button.../>` 二选一结构，触发按钮自己 `absolute right-4 top-20` 硬定位；改成"按钮永远渲染在 `group relative` 容器里，展开面板作为条件渲染的兄弟节点"，面板定位从写死的 `right-4 top-20` 改成相对自己按钮 `absolute right-0 top-full mt-2`——这样齿轮按钮能跟其余 6 个图标一起在 flex 行里正常排列，展开的设置面板始终贴着齿轮按钮自己的位置下方展开，不再依赖一个跟工具栏布局脱节的固定坐标。**`open` 默认值特意保留了 `useState(true)`**（这是这个项目最早的设置面板，一直是默认展开，用户这次没提要改这个，只说了要挪位置，不是不显示——沿用原有默认行为，没有顺手" 顺便"改成默认收起）。

  **`IconToolbar.tsx`/`page.tsx` 之间的 props 需要重新走一遍**：`MapControls` 需要的 `basemap`/`onBasemapChange`/`colorStyle`/`onColorStyleChange`/`opacity`/`onOpacityChange`/`visible`/`onVisibleChange`/`weatherOverlay`/`onWeatherOverlayChange` 这十个 props，之前一直是 `page.tsx` 直接传给独立渲染的 `<MapControls>`；现在 `<MapControls>` 挪进 `IconToolbar.tsx` 内部渲染，这十个 props 全部先加进 `IconToolbarProps`，再由 `page.tsx` 从原来"直接传给 `<MapControls>`"改成"传给 `<IconToolbar>`，`IconToolbar` 内部原样转手给 `<MapControls>`"——纯粹的一层数据透传，没有新增状态。`showDarkSkyPlaces`/`showAurora`/`showTerminator` 三个原本就已经在 `IconToolbarProps` 里（给工具栏自己的三个开关图标用），这次直接复用同一份传给 `MapControls`，不用重复声明。

  **又一次踩到"HMR 中间态残留在日志里"的假警报，这次更明确地确认了触发条件**：中间修改 `MapControls.tsx` 的过程分了两次 `Edit` 调用（第一次改开头的按钮结构，第二次改结尾补上匹配的闭合标签），两次调用之间文件在磁盘上短暂处于"JSX 标签数量不匹配"的中间状态——`preview_logs` 里翻到一条 `Expected '</', got ';'` 的 SWC 编译错误，行号精确指向那个中间态会出错的位置。这次没有像前几次一样光凭"最新几条日志是干净的 200"去交叉验证，而是直接重新 `Read` 了改动后的 `MapControls.tsx` 全文，肉眼数了一遍 `{open && ( <div>...</div> )}` 的括号和标签配对，确认落盘的最终版本结构是对的，`tsc --noEmit` 也是干净的——**两次编辑一个文件之间如果有中间不完整态，本地 dev server 的 fast refresh 很可能会抢在第二次编辑落盘前尝试编译一次并报错，这类报错的时间窗口非常窄、几乎必然是假警报，判断依据应该是"编辑操作已经全部完成后的文件内容"而不是"这次会话里出现过的任意一条报错日志"**。

  已完成，`tsc`/`lint`/生产 `build` 全过。中英文都用 DOM 查询确认了最终 7 个图标顺序不变、齿轮按钮跟历史记录图标严丝合缝并排（两者间距 8px，跟其余图标间距一致）、展开的设置面板贴齿轮按钮右边缘正下方展开（不再是写死坐标）、`page.tsx` 里不再有单独渲染的 `<MapControls>`。
- **图标工具栏第四轮调整：五个弹窗/面板互斥，同一时间只能开一个**：用户发了张截图，黄金时刻弹窗和地图控制面板同时展开、叠在一起互相遮挡，要求"打开其中一个功能的时候，另外的弹窗是会取消的"。这次动到的是月相/黄金时刻/历史记录/地图控制/最佳观星地点这五个"点了会展开浮层"的功能——三个纯开关图标（暗空地点/极光/昼夜分界线）不属于这次范围，它们本来就不是"展开面板"，是直接切换图层显隐，不会跟其他弹窗抢位置。

  **根因是这四个组件（`MoonPhasePanel`/`LocationHistoryPanel`/`MapControls`，加上 `IconToolbar.tsx` 内部的 `GoldenHourButton`）各自拿一个独立的本地 `useState` 管自己的开关状态，互相之间完全不知道对方的存在**——这是从最早只有月相面板一个悬浮功能的时候就这么写的，之后每加一个新面板（历史记录、黄金时刻、地图控制）都是照抄同一个"自己管自己"的模式复制过去，当时每个面板都单独看没问题，直到这次五个凑到同一排才暴露出"谁都不知道该让谁关"的问题。**唯一的修法是把"到底哪个面板是打开的"这个状态从各个组件里拔出来，提到共同的父组件 `IconToolbar.tsx`**：新增一个 `type ActivePopup = "moonPhase" | "goldenHour" | "history" | "mapControls" | "bestSpots" | null` 和 `const [activePopup, setActivePopup] = useState<ActivePopup>("mapControls")`（初始值仍然是 `"mapControls"`，保留地图控制面板原来默认展开的行为），`MoonPhasePanel`/`LocationHistoryPanel`/`MapControls` 三个组件从"自己拿 `useState` 管 `open`"改造成受控组件（新增 `open`/`onOpenChange` 两个 props，组件内部原来 `setOpen(...)` 的地方全部换成调用 `onOpenChange(...)`），`GoldenHourButton` 本来就是这次改造中定义在 `IconToolbar.tsx` 内部的组件，直接顺手也改成受控的，不用跨文件。父组件用 `makeOpenChangeHandler(id) = (open) => setActivePopup(open ? id : null)` 给每个面板生成各自的回调——不管是面板自己的触发按钮（会传 `!open`，即"切换"语义）还是面板内部的 ✕ 关闭按钮（会传 `false`，即"关闭"语义），父组件收到后统一按"传 `true` 就把 `activePopup` 设成这个 id（顺带挤掉原来那个），传 `false` 就设成 `null`"处理，不用关心调用方是切换还是显式关闭，语义天然兼容。最佳观星地点（`bestSpotsOrigin` 状态，决定 `BestSpotsSearchModal` 弹不弹）也折进同一套机制——点击时 `setActivePopup("bestSpots")` 顺带算好搜索圆心坐标，渲染条件从原来单纯" `bestSpotsOrigin` 存在就渲染"改成"`activePopup === "bestSpots"` 且 `bestSpotsOrigin` 存在才渲染"，这样别的面板打开时活跃 id 变了，即使 `bestSpotsOrigin` 这个坐标缓存还留着也不会被误渲染出来。

  **`LocationHistoryPanel.tsx` 撞上了这个项目已经踩过两次的同一条 ESLint 规则（`react-hooks/set-state-in-effect`）**：第一版直接写了 `useEffect(() => { if (open) setHistory(getLocationHistory()); }, [open])`，想在 `open` 变 `true` 时重新从 localStorage 读一遍最新历史——这次的场景比之前两次都更容易踩中这条规则，因为 `open` 现在是外部传入的 prop，不只是"用户点了这个面板自己的按钮"才会变 `true`，理论上也可能因为父组件的其他原因变化（虽然实际上这次不会，但组件本身没法从类型上保证），ESLint 直接在 `useEffect` 里调用 `setState` 判定为可能引发级联渲染。**修法跟上一次一样**：把"重新读 localStorage"这个副作用从 `useEffect` 挪回触发按钮自己的 `onClick` 里——`if (!open) setHistory(getLocationHistory()); onOpenChange(!open);`，只有用户真正点击这个面板自己的按钮触发"打开"意图时才读，不依赖 `open` prop 的变化去反推"是不是该读了"。**这条规则这个项目目前为止一共踩过三次**（`LocationHistoryPanel.tsx` 的对比状态、`LanguageContext.tsx` 的 `localStorage` 语言读取、这次），前两次记录在案的经验（"组件挂载时需要从外部存储同步一个值，优先想 `useSyncExternalStore` 或挪进事件处理函数，而不是 `useEffect`+`setState`"）这次直接命中，没有走弯路。

  **验证阶段又一次遇到"两次连续 `Edit` 之间的中间态被 HMR 抢先编译报错"的假警报**（这次是改 `MapControls.tsx` 加 `open`/`onOpenChange` props 的两次 `Edit` 之间，日志里出现了 `useState is not defined`/`Identifier 'open' has already been declared` 这类看似很吓人的报错），这次没有花时间重新去核对文件内容——直接靠"`tsc`/`lint` 对最终文件跑出来都是干净的"加上"紧接着做的 5 组真实点击测试全部按预期互斥"这两个更直接的证据组合，判定日志里那几条是历史噪音，比上次又快了一步。

  已完成，`tsc`/`lint`/生产 `build`（清过 `.next` 后重新 `dev` 启动）全过。用 JS 直接调用各按钮的 `.click()` 加等待（这个会话一直存在的 `document.hidden` 导致 React 调度延迟的老问题，等 2 秒再查状态）依次验证了五条真实的互斥转换：默认地图控制展开→点黄金时刻后地图控制关闭且黄金时刻展开→点月相后黄金时刻关闭且月相展开→点历史记录后月相关闭且历史记录展开→点最佳观星地点后历史记录关闭且搜索弹窗展开，中英文两个语言都跑了一遍全部符合预期，没有再出现两个浮层同时可见的情况。

- **`robots.txt` + `sitemap.xml`（Google 收录）**：用户问"怎么让谷歌收录网站"，顺手查了一下线上两个 URL 都是 404——之前只配了 canonical/hreflang 的 `<meta>` 标签，没有实际的爬虫入口文件。用 Next.js App Router 的文件约定加了 `src/app/robots.ts`（允许全部 UA，`disallow: /api/`，指向 sitemap）和 `src/app/sitemap.ts`（列 3 个页面概念 × en/zh，`alternates.languages` 字段带上 hreflang 配对，跟 `[locale]/layout.tsx` 里 `generateMetadata` 的 hreflang 逻辑保持一致）。这两个文件放在 `src/app/` 根目录（不在 `[locale]` 里面），因为它们是站点级别的、不分语言前缀的单一路由。已部署，线上 `/robots.txt`、`/sitemap.xml` 都实测确认 200。收录本身（验证域名所有权、提交 sitemap、请求编入索引）需要用户自己的 Google 账号操作，这部分给了操作步骤但没法代做。
- **`LocationDetailPanel.tsx` 加滚动**：用户反馈位置详情面板需要能上下滚动——面板本来只有 `absolute` 定位没有高度限制，随着这个项目陆续往里面加了趋势图/天气区块/曝光计算器/最佳观星地点等内容，面板早就比视口高了，只是没加滚动条所以底部内容会直接被截断看不到。加了 `max-h-[80vh] overflow-y-auto`，跟这个项目其他浮层面板（历史记录模态、黄金时刻弹窗）已经在用的封顶高度是同一个约定。用真实 Bortle 点击测试确认过 `scrollHeight`(993px) 明显大于 `clientHeight`(574px)，滚动确实生效。
- **每月晴夜比例气候统计（Clear Nights）**：用户要求"按难度做"，从上次去竞品站新发现的三个候选功能（方向性天空亮度图、交互式 3D 星空、每月晴夜比例）里挑最容易的先做——之前这个功能是因为"没有免费数据源"被跳过的，这次去查发现判断已经过时：项目里已经在用的 Open-Meteo，除了预报接口还有一个历史归档接口 `archive-api.open-meteo.com/v1/archive`，真实 curl 测过确认能拿多年历史逐时 `cloud_cover`/`is_day`，免费不需要 key。

  **算法**：复用 `weather.ts` 里已有的 `nightCloudCoverByDate()`（之前给 7 天预报按夜晚分桶云量用的纯函数，这次直接拿来处理历史数据，一个字没改）。新增 `src/app/api/clear-nights/route.ts`：对某个月份，并行请求最近 5 个"完整"自然年（`当前年-1` 到 `当前年-5`，不用今年，避免"这个月今年是否已经发生过"这种边界情况影响样本数）对应月份的历史数据，把每年每晚的夜间平均云量算出来，云量 ≤30% 记为"晴夜"（一个明确写清楚是简化阈值、不是气象学官方定义的取值），晴夜数/总夜晚数就是最终百分比。**过滤了一个跨月的"漏网夜晚"**：`nightCloudCoverByDate()` 会把请求范围第一天 00-11 点的云量归到"前一天晚上"，这天如果是月初 1 号，会漏到上个月、且只有上半夜数据（没请求上个月的傍晚数据），直接按 `date.slice(5,7) !== monthPadded` 过滤掉，不让这个不完整的半夜样本混进统计。

  **验证比预期更能自证靠谱**：拿两个气候特征完全相反的真实城市反着测——洛杉矶 8 月（地中海气候、夏季干燥少云）算出 73% 晴夜，西雅图 1 月（太平洋西北岸出了名的冬季阴雨绵绵）只有 8%——不是刻意调出来的，两个数字都完全符合真实世界常识，侧面证明简化阈值+算法方向没错。

  **前端**：接进 `LocationDetailPanel.tsx`，跟其余 4 个请求（点位取值/地名反查/趋势图/天气预报）用同一个 `useEffect` 里的 `AbortController` 并行发起，不额外拖慢其他内容的显示；渲染在天气区块之后，一个小的百分比数字 + 一行小字说明"这是气候统计估算值，不是天气预报"（跟曝光计算器、天气打分那些地方一贯的"明确写清楚是近似值"原则一致）。月份名字用 `Intl.DateTimeFormat(locale, {month:"short"})` 客户端格式化，没有为月份名新建一套翻译字典条目——日期这种东西交给 `Intl` 比手写一份 12 个月名字的翻译表更省事也更不容易出错。

  已完成，`tsc`/`lint`/生产 `build` 全过（`build` 输出确认 `/api/clear-nights` 是新增的动态路由）。中英文都在浏览器里用真实点击验证过面板正确显示"Clear Nights (Aug) 73%"及对应中文"晴夜比例（8月）73%"，数字跟独立 curl 测试完全一致。
- **方向性天空亮度图（Directional Sky Brightness）**：三个候选里第二容易的一个，用户选完就直接说"继续做"。竞品原版是"地平线/全景/穹顶"三种投影+"自然视图/SQM刻度/局部对比度"三种渲染模式的复杂图表，还带一个能拖拽旋转的独立"3D"按钮（那个是单独更难的"交互式 3D 星空"功能，这次没做）——这次做的是简化到只有一种视图的版本：一条按方位角（0°-360°）画出天空辉光强度的横向曲线图，配上"最暗天区在哪个方向"「最亮光害在哪个方向」两行文字统计，核心信息保留，投影方式的花活没做。

  **核心算法是这个项目里第一次真正做"光污染传播"建模，不是查表**：`src/lib/directionalSkyBrightness.ts`——围绕选中点，每隔 15° 方位角（24 个方向）朝外在 [10, 25, 50, 90, 140] 公里五个距离上取样 VIIRS 辐射值（`destinationPoint()` 用标准球面大地"起点+方位角+距离→终点"公式算取样点坐标，独立脚本验证过：伦敦正北 111.19km 精确落在纬度+1°、伦敦任意方向 90km 用 haversine 距离反算回来精确等于 90km，误差为 0），每个方向把 5 个距离样本按 `exp(-距离/60km)` 指数衰减加权求和，再加上观测点自己的本地辐射值（每个方向都有的"本地环境光"基线）——这是一个**明确写清楚是简化模型**的指数衰减近似，不是 Garstang 之类的真实大气散射传播模型（这个决定跟 `bortle.ts`/`bestSpots.ts`/`stargazing.ts` 一直以来的取舍原则一致：不去逆向工程一个没法验证对不对的复杂物理模型）。汇总出的"总辐射值"复用 `bortle.ts` 已有的 `radianceToBortleEstimate()` 换算成 SQM，跟应用里其余所有 SQM 数字用的是同一套换算，不是另起一套。

  **后端**：`src/app/api/directional-sky-brightness/route.ts`，观测点本身作为一个特殊"哨兵方位角"（`CENTER_SENTINEL_AZIMUTH = -1`）样本混进同一批 EE `sampleRegions` 请求里一起取（24×5+1=121 个点，比 `best-spots` 最多 900 个点的量级小得多），这样"本地环境光基线"不需要客户端已经拿到 `/api/point-value` 的结果才能算——跟"曝光计算器评分算在前端、天气接口只吐原始数据"那次的设计考量一样：保持这个面板一贯的"全部请求并行发起互不等待"结构，不引入新的请求依赖链。

  **验证比预期更能自证靠谱，用了地理条件本来就该产生真实方向性差异的真实地点交叉检验**：马里布海岸（西边太平洋、东边洛杉矶市区）——朝西（270°方向，海洋一侧）算出 SQM 20.15（波特尔 5 级，明显暗），朝东（90°方向，洛杉矶一侧）算出 SQM 18.11（波特尔 7 级，明显亮）；反过来在洛杉矶市中心测（四面都是同一片超大城市，物理上不该有明显方向性差异）算出的 24 个方向 SQM 全部挤在 17.06-17.52 一个很窄的区间——两个地点的结果分别符合"沿海该有方向性差异"和"市中心不该有方向性差异"这两个相反的真实世界预期，不是凑出来的巧合。

  **前端**：`src/components/DirectionalSkyBrightnessChart.tsx`（纯展示组件，接 `directions`/`darkest`/`brightest` props）——SVG 折线+面积图，X 轴 0°-360° 方位角，Y 轴复用 `bortle.ts` 本来就在用的 SQM 16-22 范围（跟应用里其它 SQM 数字视觉刻度一致），暖色（琥珀色）代表光害辉光，N/E/S/W 四条参考线；下方两行"最暗天区：方位+SQM"「最亮光害：方位+SQM」文字统计，方位角转 16 方位罗盘缩写（N/NNE/NE...）的小函数是从 `GoldenHourCalculator.tsx` 里已有的同名逻辑直接复制的一份小写（4 行代码，没有为了复用单独抽一个共享文件——两处独立小重复比额外抽象更省事）。接进 `LocationDetailPanel.tsx` 第 6 个并行请求，渲染位置紧跟在"波特尔/SQM/可见恒星数"之后、趋势图之前，对齐竞品原版的实际展示顺序。

  已完成，`tsc`/`lint`/生产 `build` 全过（`build` 输出确认 `/api/directional-sky-brightness` 是新增的动态路由，单次请求耗时约 2 秒，量级跟 `best-spots` 相近）。中英文都在浏览器里用真实点击验证过图表 SVG 里的折线/面积坐标点是合法数字（不是 NaN/空），文字统计"Darkest sky: SSW 195° · SQM 20.32"「最暗天区: SSW 195° · SQM 20.32」跟独立 curl 测试的原始 JSON 完全一致。
- **FAQ 意见反馈入口 + `/about-data` 页面 + 位置详情滚动条配色**：一次提了三个不相关的小需求，分开记。

  **FAQ 反馈入口**：先去竞品站实测——`InfoPanel.tsx` 对应的竞品 FAQ 面板一路滚到最底才找到，是一个不起眼的"Feedback"小节：一个"About & Data"链接 + 三个社交媒体账号（X/Bluesky/Facebook）。这个项目没有社交账号，直接把三个社交链接换成用户自己的邮箱 mailto 链接（`morning.seven.77.77.77@gmail.com`），加在 `InfoPanel.tsx` 现有 FAQ 列表下方、`border-t` 分隔的新区块里，同样是"About & Data"链接（指向新页面）+ 邮箱两行，位置和结构照抄竞品，内容换成这个项目真实拥有的联系方式。

  **`/about-data` 页面**：跟 `golden-hour`/`rankings` 一样的 `layout.tsx`（`generateMetadata`）+ `page.tsx`（`"use client"` + `SiteHeader` + 返回地图链接）结构，加进了 `sitemap.ts` 的 `PAGES` 列表。**内容不是照抄竞品原文，是参考竞品的板块结构、换成这个项目真实的数据情况写的**——竞品原版有"2025 完整年层 / 2026 预估层"两张卡片，这个区别是他们自己的月度更新流水线才有的东西；先去查了 `earthEngine.ts` 里 `getViirsImage()` 实际用的年份范围（`LATEST_YEAR_START/END = "2024-01-01"/"2025-01-01"`），确认这个项目只有单一一个"2024 年度层"，没有"预估层"这个概念，所以这次做成了一张卡片而不是两张，如实反映项目自己的数据状态，没有编造一个不存在的"预估层"出来对齐竞品结构。"数据是怎么做出来的"三步卡片里，第二步"天空亮度换算模型"特意写清楚是简化对数公式、不是 `bortle.ts` 注释里提到的 Falchi-Cinzano《世界光污染地图集》那种真实大气辐射传输模型——这句话直接复用了 `bortle.ts` 顶部已有注释的原意，不是新编的说法。"其它估算功能"一节点名列出了这个项目自己做过的几个"明确标注为简化估算"的功能（夜间天气评分/曝光计算器/晴夜比例/方向性天空亮度），算是把散落在各处的"这是近似值"说明在一个地方汇总一遍。"责任与联系方式"没有照抄竞品"Stargazing Hub Team"这种听起来像正经团队的说法——这个项目就是用户自己的练手项目，写成"独立、非商业的练手项目"更如实，联系方式换成同一个邮箱地址。

  **位置详情面板滚动条配色**：`overflow-y-auto` 之前一直用浏览器默认滚动条（多数系统下是亮色/灰色，跟面板本身的深色 `zinc-900` 背景很不搭）。在 `globals.css` 里加了一个 `.themed-scrollbar` 工具类——同时写了 Firefox 标准的 `scrollbar-width`/`scrollbar-color` 属性和 WebKit 系的 `::-webkit-scrollbar*` 伪元素规则（这两套是两个完全独立的机制，没有一条 CSS 规则能同时覆盖两边，必须都写），滚动条本体用半透明白色（`rgba(255,255,255,0.18)`，悬停时 `0.32`）配透明轨道，跟面板已有的 `border-white/10` 那种"半透明白"配色语言保持一致，没有另起一套新颜色。只加在 `LocationDetailPanel.tsx` 这一个面板上（用户只提到这一个），没有顺手扩散到其它可滚动面板（历史记录模态、黄金时刻弹窗、排行榜表格等）——如果以后要统一改，直接把同一个类名加上去就行，类本身是通用的。

  已完成，`tsc`/`lint`/生产 `build` 全过（`build` 输出确认 `/about-data` 是新增的 SSG 页面，`/en/about-data`、`/zh/about-data` 都在列）。中英文都在浏览器里验证过：FAQ 面板底部"意见反馈"区块的两个链接 `href` 分别正确指向 `/zh/about-data`（locale-aware）和 `mailto:morning.seven.77.77.77@gmail.com`；`/about-data` 页面完整内容正确渲染；位置详情面板的 `themed-scrollbar` 类通过 `getComputedStyle` 实测确认 `scrollbarColor`/`scrollbarWidth` 两个属性都生效（不只是类名挂上去了，样式规则真的匹配到了）。

- **顶部导航"更多"下拉菜单 + 晴夜气候地图独立落地页（`/clear-sky-map`）**：用户发了竞品"Resources"下拉菜单的截图问里面都是什么，翻了一遍发现里面 4 个真正的落地页（Golden Hour、Clear-night climate、Global Light Pollution Rankings、About & Data）里，这个项目只有黄金时刻和排行榜做成了独立可收录页面，晴夜比例功能虽然早就做了（`LocationDetailPanel.tsx` 里的小字百分比）但从没有自己的落地页，关于本站数据的内容也只在 FAQ 里。用户接着要求照竞品这种"下拉菜单收纳几个落地页"的设计，在语言切换器左边加一个新按钮（名字自己起，用户觉得"Resources"跟这个项目的功能组合对不上），点开是 Golden Hour/Clear-night climate/排行榜/About&Data 四项，原来单独放在顶部导航的排行榜链接也收进这个菜单。

  **新按钮命名为"More"/"更多"**（不是照搬"Resources"）——这个项目的四个落地页是计算器+气候统计+数据表+关于页的混合体，不完全是"资源"这个词通常指的文档/下载类内容，`siteHeader.moreMenuLabel`/`moreMenuAria` 是新增的两个翻译 key。`SiteHeader.tsx` 新增 `MorePagesMenu()` 组件，跟旁边的 `LanguageSwitcher()` 是同一套"点击展开+全屏透明遮罩点外部关闭"下拉模式（两者一直挨在一起，直接照抄现成模式没有另起一套），4 个 `Link` 项复用各自页面早就有的 `navLabel` 翻译 key（`goldenHour.navLabel`/`clearSkyMap.navLabel`/`rankings.navLabel`/`aboutData.navLabel`），没有为菜单项另外新建一套文案。原来 `SiteHeader.tsx` 主函数里单独渲染的"🌍 Rankings"链接删掉，替换成 `<MorePagesMenu />`。

  **`/clear-sky-map` 落地页新建，复用了已有的 `/api/clear-nights` 接口和月度晴夜比例算法，不是新写后端逻辑**——页面本身是"位置搜索 + 12 个月并行请求 + SVG 柱状图 + 最晴/最多云月份文字统计 + 数据说明"，跟 `golden-hour`/`about-data` 一样的 `layout.tsx`（`generateMetadata`）+ `page.tsx` 结构。新增 `src/components/ClearSkyMonthChart.tsx`（纯展示 SVG 柱状图组件，12 根柱子，某个月份数据缺失时渲染成半透明灰色空柱而不是跳过或崩溃，最晴/最多云月份的统计只从有数据的月份里选）。

  **顺手把 `GoldenHourCalculator.tsx` 和新页面共用的"位置搜索"UI 抽成了 `src/components/LocationSearchField.tsx`**——"使用我的位置"按钮 + 防抖 `/api/geocode` 搜索框这段逻辑（约 50 行、带真实的 state/effect）两处几乎一模一样，超过了这个项目一直以来"小重复保留、大重复才抽"的阈值（比如 `azimuthToCompassPoint` 这种 4 行小函数一直是故意保留两份不抽的）。配套把 `messages/*.json` 里原本挂在 `goldenHour`命名空间下的 `useMyLocation`/`locatingLabel`/`geolocationError`/`locationPlaceholder`/`searching`/`noResults` 六个 key 挪到新的共享命名空间 `locationSearch` 下，两个页面都改成读这个共享命名空间。`GoldenHourCalculator.tsx` 改造后重新完整验证过一遍（搜索/定位/日期/罗盘/结果全部正常），确认这次重构没有破坏这个已经上线的功能。

  **`react-hooks/set-state-in-effect` 这条规则第四次命中**：`clear-sky-map/page.tsx` 一开始跟 `golden-hour` 早期版本一样，在 `useEffect` 里先同步调一次 `setMonthly({status:"loading"})` 再发起 12 个并行请求，想在切换地点时把状态重置回"加载中"。**这次用的修法是 `LocationDetailPanel.tsx` 一直在用、但这个项目其他几次踩坑时没有直接复用的第三种解法**：把 12 个月请求的逻辑拆进一个按 `location` 坐标 `key` 重新挂载的内层组件 `MonthlyClearSkyResults`，`useState({status:"loading"})` 的初始值本身就承担了"切换地点后重置成加载中"的语义，`useEffect` 里只保留发请求和 `.then()`/`.catch()` 里的 `setState`（这些是异步回调里调用，不会触发这条规则）。**这条规则目前为止在这个项目里一共命中过 4 次**（`LocationHistoryPanel.tsx` 对比状态、`LanguageContext.tsx` 的 localStorage 读取、`IconToolbar.tsx` 互斥改造时的 `LocationHistoryPanel.tsx`、这次），前三次总结的三种解法（挪进事件处理函数 / `useSyncExternalStore` / key-remount）加起来基本覆盖了这个项目里会遇到的所有场景，以后再撞见先看是哪一种再挑对应解法，不用现想。

  **验证**：`tsc`/`lint`/生产 `build` 全过（`build` 输出确认 `/en/clear-sky-map`、`/zh/clear-sky-map` 都是新增的 SSG 页面）。中英文都在浏览器里验证过：`更多`/`More` 下拉菜单打开后 4 个链接的 `href` 都正确带 locale 前缀；`/clear-sky-map` 搜索东京后 12 个月真实数据正确渲染（1 月最晴 51%、6 月最多云 11%，符合东京梅雨季的真实气候常识），个别月份因为 Open-Meteo 上游瞬时 502 返回 null 时柱状图正确降级成灰色空柱、不影响其余月份和最晴/最多云统计的显示；`golden-hour` 页面重构后重新搜索巴黎验证了日出日落/黄金时刻/蓝调时刻/罗盘方位角数据全部正确，确认共享组件抽取没有破坏原有功能。
- **排行榜每个国家的年度趋势面板**：用户发了竞品排行榜展开某个国家后的截图（6 个统计小卡片 + SQM 多年趋势折线图 + Bortle 1-9 面积加权分布条），要求也做一个。跟排行榜表格本身（离线预算好的单年快照）不一样，这次是**按需查询**——点开某一行才现算这个国家的多年数据，问过用户"全部离线预算 vs 按需查询"后用户选按需（离线预算 281 国 × 13 年是数小时级别的批量任务，按需只需处理真正被点开的国家）。

  **新增 `src/app/api/country-trend/route.ts`**：`GET ?name=<国家名>`，用 `LSIB_SIMPLE` 按 `country_na` 过滤出这个国家（`.geometry()` 直接把跨海多块陆地的同名 feature 合并成一个几何体，比 `countryRankings.ts` 离线脚本当初"批量算完再按 pixelCount 加权合并"的做法更直接——因为这次只查一个国家，不需要跨 feature 批量处理）。多年趋势用 `getViirsTrendCollection().map(image => Feature(null, reduceRegion(mean+count)).set('year', ...))` 包成一个 `FeatureCollection` 一次 `getInfo()` 拿全部年份（EE 常见的"整段时间序列一次往返"写法，跟已有的单点 `/api/trend` 用 `getRegion()` 一次拿全部年份是同一个思路，只是这次多年 × 一整个国家区域，得用 `reduceRegion` 而不是单点的 `getRegion`）。Bortle 1-9 分布复用 `tile-layer/route.ts` 里"`image.gte(breakpoint)` 累加分类"那套技术（这次起始值从 0 改成 1，直接得到真实波特尔级数），配 `ee.Reducer.frequencyHistogram()` 一次性拿到 9 个类的像元数，不用 9 次单独 reduce。

  **单一固定的 scale/simplify 参数没法同时伺候 Bahrain 和俄罗斯，踩了两轮才找到真正的瓶颈**：先按真实接口反复测时间——`scale=10km` 无 simplify 时 Bahrain 3 秒但俄罗斯整整 2 分钟；单纯把 `scale` 放粗到 30km（像素数少了 9 倍）俄罗斯只从 120s 降到 100s，说明瓶颈根本不是像素数，而是**每年一次 `reduceRegion` 对俄罗斯几千个顶点的复杂国界做裁剪的开销**，跟 scale 无关；加一次 `geometry.simplify()`（容差设到跟 zonal scale 一样，反正细节精度到不了一个像素也没意义）后俄罗斯直接从 100s 掉到 19-29s，这才是真正起作用的杠杆。但接着**同一个 simplify 容差用在 Bahrain 这种几十公里见方的小国上直接把结果搞坏了**——30km 简化容差下 Bahrain 的 `pixelCount` 从真实的 27 掉到 6、`avgSqm` 整整偏了一个多星等，因为国土范围本身跟简化容差同一个量级，简化直接削掉了大半个国家。**最终方案是按国家大小分三档**（`pickZonalTier`，用 `countryRankings.ts` 里现成的离线 `pixelCount` 当"国家大小"的免费代理指标，不用另外发一次面积查询）：小国（<2000 像素）用 `scale=3km/simplify=300m`、中等国家用 `10km/1km`、俄罗斯/加拿大这类超大国家用 `30km/3km`。三档都用真实数据核对过（法国 21.46 精确对上离线快照、Bahrain 19.20 对上离线快照的 19.23、梵蒂冈这种全国只有 1 个原生像素的极端情况也没崩），响应时间从几秒（大多数国家）到俄罗斯的 17-29 秒不等。**教训**：给一个跨越 7 个数量级国土面积（梵蒂冈 0.44km² 到俄罗斯 1700 万 km²）的数据集选统一参数，本质上是在缘木求鱼——用已有的、免费的规模代理指标分档，比试图找一个"万能"的中间值更对。

  **前端**：`src/components/CountryTrendPanel.tsx`——6 个统计小卡片（最新年份/SQM/亮度倍数/Bortle≤4/年份跨度/像元数，亮度倍数复用 `locationHistory.ts` 已有的 `computeSkyBackgroundRatio`）+ SQM 折线图（跟 `DirectionalSkyBrightnessChart.tsx` 同款 SVG polyline+渐变面积写法，只是 X 轴从方位角换成年份）+ Bortle 1-9 横向条形分布图（颜色复用 `colorStyles.ts` 里"经典"风格已有的 `paletteToBortleSwatches()`，跟地图上 Bortle 图例用的是同一组颜色）。`rankings/page.tsx` 每行前加一个展开按钮，点击时**直接在 `onClick` 里 `fetch`**（不是 `useEffect`），按国家名缓存结果避免重复查询——这类由用户点击触发、按需发起的请求本来就不会撞上 `react-hooks/set-state-in-effect`，不需要套用前面几次踩坑总结的任何一种解法。

  已完成，`tsc`/`lint`/生产 `build` 全过（`build` 确认 `/api/country-trend` 是新增的动态路由）。中英文都在浏览器里搜索 Bahrain 展开验证过，6 个统计卡片/折线图/9 条 Bortle 分布条全部渲染真实数据且跟独立 curl 测试完全一致；`preview_logs` 服务端日志确认接口真实调用成功（`GET /api/country-trend?name=Bahrain 200 in 2.8s`），期间浏览器控制台出现的一次 SWC 语法报错核对后确认是这个项目已经记录过的"两次连续 `Edit` 之间的中间态被 HMR 抢先编译"假警报（改 `rankings/page.tsx` 从 `<>...</>` 换成 `<Fragment key=.../>` 分两步落盘时短暂不匹配），最终文件内容和 `tsc`/`lint` 结果都是干净的。

## 已知坑点（踩过的坑，遇到类似问题不用重新排查一遍）

- **maplibre-gl v6 地图纯黑不渲染**：v6 是纯 ESM 包，打包工具解析不了它内部 Web Worker 的脚本地址，必须手动 `setWorkerUrl()`（已在 `Map.tsx` 里处理）。worker 文件还用相对路径 import 了一个同目录的 `maplibre-gl-shared.mjs`，不能指向打包工具生成的带 hash 文件名路径，所以用 `scripts/copy-maplibre-worker.mjs` 在 postinstall 时复制到 `public/maplibre/` 保持原文件名。
- **图层遮住地图文字**：`map.addLayer()` 不传第二个参数会把新图层加到最顶层，盖住底图的文字标注。要用 `map.getStyle().layers.find(l => l.type === "symbol")?.id` 找到第一个文字图层传进去。
- **VIIRS 瓦片像素马赛克感**：原始分辨率约 463m/像素，缩放到街道级别很明显。用 `image.resample("bilinear")`（只用于生成瓦片图那次调用，不影响 `point-value` 用的原始像素值）能让色块平滑过渡。
- **Earth Engine `getMap()`/`visualize()` 不允许同时传 `gamma` 和 `palette`**，会直接报错。
- **`@google/earthengine` 这个包的 Node 客户端完全不支持走代理，分两条独立链路，都得单独修**（不再需要开 TUN 模式）：
  - **数据层请求**（瓦片、取值等）：内部用老式 `xmlhttprequest`，请求写死 `agent: false`。`src/lib/earthEngineTransport.ts` 把 `xmlhttprequest` 包导出的构造函数换成一个用 `fetch` 实现的替身。**关键坑**：`@google/earthengine` 的 `main.js` 在自己模块顶层用 `const XMLHttpRequest = require("xmlhttprequest").XMLHttpRequest` 把类**缓存成一个常量**（只在这个包第一次被 require 时执行一次，不是每次用的时候才读）。只在某个 API 路由自己的文件里 `import "./earthEngineTransport"` 不够早——实测发现 Next.js 在真正处理请求之前的某个阶段（应该是路由编译/分析）就已经把 `@google/earthengine` require 过一次，那次拿到的还是原始未替换的类，之后不管怎么改路由文件里的 import 顺序都没用。**真正的修复是把这个 import 挪到 `src/instrumentation.ts` 的 `register()` 里**——这是 Next.js 保证全局最早执行的地方，比任何路由模块都早。另外 `next.config.ts` 里给 `@google/earthengine` 和 `xmlhttprequest` 都加了 `serverExternalPackages`，让它们走 Node 真正的 `require()` 缓存而不是被 webpack 按路由各打一份包（避免同一个包在不同路由里出现两份不同的模块实例）。顺带抓到一个真 bug：`xmlhttprequest` 对不带 body 的 GET 请求会调用 `send("")`，但原生 `fetch()` 只要 GET/HEAD 请求带了 body（哪怕是空字符串）就直接抛错，得转成 `body ? body : undefined`。
  - **认证请求**（拿 access token）：`ee.data.authenticateViaPrivateKey` 内部用 googleapis 的 `google.auth.JWT`，走 `gaxios`，跟上面那条链路完全独立，不受 xmlhttprequest 补丁影响。`gaxios` 会根据 `HTTP_PROXY`/`HTTPS_PROXY` 构造一个 Node 风格的 `HttpsProxyAgent`，但它实际发请求用的是 Node 原生 `fetch`——原生 `fetch` 根本没有 `agent` 这个选项，这个代理配置被静默丢弃，认证请求直接走直连，在需要代理才能连 Google 的网络下必然失败。**修复：不走 `authenticateViaPrivateKey`，改成自己手写 RFC 7523 JWT-bearer token 交换**（`earthEngine.ts` 的 `getGoogleAccessToken`，用 Node `crypto.createSign` 签名 + `undici` 包自己的 `fetch`/`ProxyAgent`），拿到 token 后用 `ee.data.setAuthToken(...)` 直接灌给 EE，完全绕开 gaxios。这里也有个坑：**必须用 `undici` npm 包自己导出的 `fetch` 搭配它自己的 `ProxyAgent`**，不能把 `undici` 的 `ProxyAgent` 塞给 Node 原生 `fetch`（`instrumentation.ts` 里 `setGlobalDispatcher` 设的默认代理同理不可靠）——原生 `fetch` 内部用的是 Node **自带**的 undici 版本，跟 `node_modules` 里装的 `undici` npm 包版本不一定一致，混用会报 `InvalidArgumentError: invalid onRequestStart method`。两个函数都通过 `earthEngineTransport.ts` 导出的 `getUndiciClient()` 复用同一份 fetch+dispatcher。
- **`reduceResolution` 的 `maxPixels` 调太高会让瓦片慢到没法用**：给 VIIRS 瓦片加 `reduceResolution({reducer: max, maxPixels})` 解决"世界视图下颜色稀疏成一个个光点"的问题时，一开始把 `maxPixels` 从默认的 64 调到 1024（想让低缩放级别聚合更多原始像素，效果更满），代价是每个瓦片请求都要在 Earth Engine 服务端实际聚合这么多候选像素——实测缩到最低几级时单个瓦片能卡到 27 秒，用户反馈"渲染速度太慢"。回头用同一个瓦片对比 `maxPixels=64` 和 `1024` 的实际画面，肉眼几乎看不出差别（世界视图那个缩放级别真实需要聚合的像素数是几万起步，1024 跟 64 一样都只是杯水车薪，`bestEffort` 兜底后的效果趋同），但耗时差了 8 倍以上。**最终改回默认值 64**，`bestEffort: true` 还是要留着（64 这么小的上限，会比 1024 更早触发"聚合像素数超限"要求兜底，不是可以省的）。**教训**：调整这类会影响服务端计算量的参数，不能只看"改大了效果会不会更好"，要么直接测真实耗时，要么反过来想"这个上限相对于实际需要聚合的像素数是不是杯水车薪"——如果本来就远远不够用，调大也只是烧钱不烧出效果。
- **`git push` 卡死不报错**：如果这台电脑是新装的 `gh` CLI，`gh repo create --push` 能成功是因为它自己处理认证，但后续单独 `git push` 会因为 git 没配置凭证助手卡在等一个不会来的密码输入。解决：跑一次 `gh auth setup-git`。
- **maplibre-gl `Marker` 先 `addTo()` 再 `setLngLat()` 会报错**：`Cannot read properties of undefined (reading 'lng')`。`addTo(map)` 内部要读 marker 自己的经纬度算初始屏幕位置，所以必须先 `.setLngLat()` 再 `.addTo(map)`（链式调用顺序：`new Marker().setLngLat([lng,lat]).addTo(map)`）。
- **Nominatim 反向地理编码查不到地名时返回 200**：不是失败状态码，响应体里是 `{"error":"Unable to geocode"}`，要当正常的"无地名"结果处理，不能当成上游请求失败。
- **多套配色风格共用一个"固定范围"时，min/max 选太宽会让所有风格看起来都一样**：给"高亮/辉光/科研"三个风格配一样的固定辐射范围时，一开始照着参考站图例显示的"SQM 22→17"反推出 min=0/max≈315——这个范围在数学上没错，但 `bortle.ts` 自己的辐射→SQM 换算只是个简化近似（不是参考站用的真实大气模型），按这套近似公式，SQM 17 对应的辐射值≈315，而真实点位（`point-value` 测过纽约≈211、东京≈41）几乎都远低于这个数，导致大多数地点的辐射值只落在渐变条最暗的 10%-20% 区间，三个风格切换起来跟没变一样（用户反馈"另外三个配色风格都没变化"）。**教训**：选固定范围时要用真实测过的点位数据反推一个"实际够用"的 max（这次改成 max=120），而不是死抠参考站图例上显示的具体数字——我们的换算公式本来就是近似值，图例上显示的 SQM 数字准不准是次要的，色彩在真实数据下有没有可见差异才是重点。
- **从竞品站"抠"精确调色板别靠肉眼看截图，直接抓它自己的瓦片数据**：一开始给"科研"配色按截图肉眼估的十六进制色值，用户反馈想要更准的。lightpollutionmap.app 的光污染图层实际是 Leaflet + 服务端渲染好的栅格 PNG 瓦片（不是 WebGL/MapLibre，也不是前端 JS 算的颜色——client 打包里搜"scientific"/"palette"都搜不到，因为调色板逻辑整个在后端），瓦片 URL 规律是 `https://api.lightpollutionmap.app/api/lightpollution/style-tiles/{styleId}/{年}/{zoom}/{x}/{y}.png`（在浏览器里切换地图样式后翻一下 Network 面板里带 `image-tiles`/`style-tiles` 的请求就能看到）。这类图片是跨域但没做防盗链限制，可以直接在页面 JS 里 `fetch()` 拿字节、`createImageBitmap()` 解码、画到 `OffscreenCanvas` 上、`getImageData()` 读真实像素——不会被 canvas 污染（tainted canvas）挡住。比反复截图再肉眼估色准得多，也比在竞品自己的前端代码里翻半天找不到（因为它压根不在前端）更快。

- **DarkSky 认证地点数据没有公开的一次性下载接口，得拼三个来源**：darksky.org 是 WordPress + FacetWP 插件驱动的地图（不是 API 网站）。①经纬度+post_id：地图列表页服务端渲染时会把全部地点（不受分页限制，FacetWP 地图 facet 就是这么设计的）内联进页面的 `window.FWP.settings.map.locations`，直接在页面里 `fetch(listPageUrl)` 拿 HTML 用 `"map":{"locations":` 这个标记去掰出这段 JSON 就行，不用等页面渲染。②名称+分类：`darksky.org/wp-json/wp/v2/darksky_place`（WordPress 自带 REST API，标准分页）+ `darksky.org/wp-json/wp/v2/idsp_type`（分类法术语表，用 `count` 字段能分清哪 5 个是真正的官方分类，其余是遗留/重复的空词条）。③认证年份+保护面积：这两个字段是 ACF 自定义字段，**REST API 里 `acf` 字段是空的**（没有为公开访问注册 REST 暴露），只能从每个地点自己的详情页 HTML 里正则抠——新旧地点的 HTML 结构不一样（`<h3 class="idsp">Designated</h3><p>年份</p>` vs 新版 Gutenberg 区块 `<h3 class="wp-block-heading">Designated</h3>...<p class="wp-block-paragraph">年份</p>`，中间还夹了 spacer div），正则得写成"找到标题文字后，往后 300 字符内找第一个 `<p>`"这种宽松匹配才能两种都吃住，第一版死板匹配漏了 102/292 条。**关键点**：这类请求必须在浏览器页面 JS 上下文里用 `fetch()` 发（用 `mcp__claude-in-chrome` 之类能操作真实浏览器的工具），不能用 `curl`/Node 的 `fetch` 直接打——darksky.org 有 Cloudflare 反爬挑战，服务端直连拿到的是"Just a moment..."挑战页，只有已经通过挑战、带着有效 cookie 的真实浏览器会话才能拿到真内容。
- **claude-in-chrome 的“真实 Chrome”和这台开发机不是同一台机器**：`http://localhost:3000` 在 claude-in-chrome 里打开会连到*用户自己电脑*上占用 3000 端口的另一个完全无关的本地服务，不是这个项目的 dev server；同理开发机上临时起的本地 HTTP server 也无法从 claude-in-chrome 里 `fetch` 到。抓取第三方公开网站（如 darksky.org）该用 claude-in-chrome，验证本项目自己的 dev server 该用 `mcp__Claude_Browser__*`（跟 dev server 同一台机器），两者不能换着用。
- **`next build`（生产构建）跑完之后紧接着 `next dev`（开发模式），复用同一个 `.next` 目录会导致开发服务器炸出一堆"Module parse failed"假报错**（这次是 `messages/zh.json` 报"Cannot parse JSON"，但文件在磁盘上、`node -e "JSON.parse(...)"` 直接验证完全合法——是假象，不是真的 JSON 坏了）。根因是 `.next/dev/` 和生产构建产物的内部清单/模块图结构不兼容，`next build` 生成的是生产模式的 chunk 布局，`next dev` 期待的是开发模式的（比如 JSON 消息文件在开发模式下是通过 `require.context` 动态懒加载的，生产模式走的是完全不同的打包路径）。混着复用会导致开发服务器读到跟自己模式对不上的清单，报出一堆看似指向具体某个文件内容坏掉、实际上跟那个文件毫无关系的"幽灵错误"。**修法**：只要中间跑过一次 `next build`，切回 `next dev` 之前必须先 `rm -rf .next` 再启动，不能图省事直接切换命令复用缓存——这跟已经记录过的"dev server 运行中删 `.next` 会炸"是同一类"`.next` 缓存状态和当前运行模式必须匹配"的问题，但触发条件不同（这次是模式切换，不是运行中删除），要分开当两条坑记。另外注意：`mcp__Claude_Browser__read_console_messages` 返回的是这个浏览器标签页从打开以来累积的完整控制台历史，不是"当前这次导航"的日志——遇到报错先看是不是切换服务器/重新构建之前遗留的旧记录，可以对照 `preview_logs` 里服务器自己的实时日志（如果服务器端没有对应错误，控制台里的多半是客户端缓存出的旧账），或者干脆结合 `get_page_text` 直接看页面实际渲染内容有没有问题，比死磕一条不确定还在不在生效的控制台报错更可靠。
- **`mcp__Claude_Browser__computer` 的 `screenshot` 在某些会话里会一直报"pane 未显示，没有在合成帧"**：这种状态下 MapLibre 依赖 `requestAnimationFrame` 的渲染管线也会跟着卡住（`document.hidden` 为 `true`），`map.on("load", ...)` 永远不触发、`queryRenderedFeatures` 永远查不到东西，跟代码本身对不对没关系。遇到这种情况没法在这个会话里肉眼过一遍新加的地图图层，只能靠：手动在 `window.__map`（临时在 `Map.tsx` 挂一行 `if (process.env.NODE_ENV==="development") window.__map = map` 调试完记得删）上重放 `addSource`/`addLayer`/`queryRenderedFeatures` 验证图层配置本身没语法错误，加上 DOM 层面的 UI（开关状态、图例文字）用 `get_page_text`/`read_page` 照样能查，两者结合起来对代码正确性有信心之后，把肉眼视觉核对这一步交给用户在自己电脑上确认。

## 注意事项

- Earth Engine 服务账号密钥用 `EARTH_ENGINE_SERVICE_ACCOUNT_KEY_B64` 这个环境变量（base64 编码后的完整 JSON key），放在 `.env.local`（已在 .gitignore 中排除，绝不提交到仓库）。本机之外的新环境需要单独配置这个变量（Vercel 在项目设置里配，本地开发就建 `.env.local` 文件）。
- 天气叠加（云层/降水）图层需要 `OPENWEATHERMAP_API_KEY` 环境变量，同样放 `.env.local`。去 https://openweathermap.org/ 免费注册账号，在账号设置里的 API keys 页面生成一个（免费额度对这种个人项目足够用，通常几分钟到一小时内新 key 才会激活生效，注册后如果立刻测试报错先别慌，等一会儿再试）。没配这个变量时 `/api/weather-tiles` 会返回 503，地图上选"云层"/"降水"不会报未处理异常，就是看不到图。
- `npm run dev` / `npm run build` 都带了 `--webpack` 参数，故意不用默认的 Turbopack（排查 maplibre 渲染问题时发现的兼容性顾虑，webpack 更稳）。
